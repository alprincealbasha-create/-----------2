# خطة Codex الفعلية — Rawdat Wird

ينفذ المشروع عبر بوابات مرتبة، وليس عبر طلبات برمجية متفرقة. لا تبدأ بوابة قبل اكتمال متطلبات خروج البوابة السابقة واعتمادها.

## حالة المشروع

**حالة البوابات بعد اعتماد المالك وإغلاق المرحلة 3 بتاريخ 2026-09-07:**

- `G0 = APPROVED / CLOSED / FROZEN`
- `G1 = APPROVED / CLOSED`
- `G2 = APPROVED / CLOSED`
- `G3 = APPROVED / CLOSED`
- `G4–G12 = NOT OPENED`

يمنع في G0 إنشاء أو تعديل كود Flutter، أو إنشاء ترحيلات قاعدة بيانات، أو إعادة هيكلة الملفات، أو بناء APK. يسمح فقط بمراجعة الوثائق، وتسجيل التعارضات والقرارات، واقتراح المعمارية وخطة التنفيذ.

الكود والترحيلات الموجودة حاليًا تعد نموذجًا أوليًا ومرجعًا للتعلم، وليست دليلًا على اجتياز البوابات الجديدة. لا تُحذف أو تُعاد كتابتها قبل اتخاذ قرار صريح في G0 حول الترحيل التدريجي أو إعادة البناء.

## قواعد البوابات

1. تنفذ البوابات بالترتيب من G0 إلى G12.
2. لكل بوابة نطاق واضح، واختبارات، ومعيار خروج.
3. لا تنفذ خصائص بوابة مستقبلية مبكرًا.
4. أي تعارض معماري يعيد العمل إلى البوابة المناسبة بدل ترقيعه في بوابة لاحقة.
5. لا تعد البوابة مكتملة لأن الكود كتب فقط؛ يلزم نجاح فحوصها وتحديث وثائقها.
6. لا يستخدم `service_role` داخل Flutter في أي بوابة.

## G0 — التخطيط

**ممنوع البرمجة.**

الأهداف:

- مراجعة `PRODUCT_REQUIREMENTS.md` و`DATABASE_SCHEMA.md` و`ARCHITECTURE.md`.
- مقارنة المخطط المستهدف بالتنفيذ الأولي الحالي.
- اكتشاف التعارضات والتكرار والعلاقات غير المحسومة.
- تحديد القرارات الناقصة وأثر كل خيار.
- اعتماد Architecture قابلة للتنفيذ وخطة ترحيل أو إعادة بناء.
- تحديد اختبارات القبول والأمان لكل بوابة.

حالة القرارات:

- حسم المالك OD-001–OD-013 في `OWNER_DECISION_REGISTER.md` وG0-OD-03.
- تفاصيل lockout التقنية مؤجلة إلى G3، ومدة grace window إلى G7، وقيم النقاط إلى G9، ومدد الاحتفاظ إلى G11.
- غياب حالة الإنتاج `NOT VERIFIED`. يمنع clean/destructive rebuild أو reset أو overwrite أو حذف أي حالة مجهولة، بينما يسمح مستقبلًا بعمل G1 غير الهدام وبيئات تطوير/اختبار معزولة.
- أصدر المالك قرار البوابة النهائي `GD-G0-FINAL`. أصبحت خطوط G0 مجمدة، وأي تغيير جوهري يحتاج change control مع أثر downstream.
- فتح G1 لا يفتح G2، ولا يجيز تجاوز متطلبات G1 أو قيد OD-012.

معيار الخروج:

- لا توجد قرارات مخطط حرجة معلقة.
- توجد مصفوفة أدوار وصلاحيات RLS معتمدة.
- توجد خريطة جداول وعلاقات نهائية.
- توجد خطة ترحيل مكتوبة وقابلة للعكس أو خطة إعادة بناء معتمدة.
- تعتمد معايير قبول G1–G12 قبل بدء البرمجة.

## G1 — إنشاء المشروع

النطاق: مشروع Flutter، الحزم المعتمدة، الإعدادات، والبنية الأساسية للمجلدات دون خصائص منتج.

معيار الخروج: تشغيل التطبيق الأساسي، نجاح `flutter analyze` و`flutter test`، وعدم وجود أسرار في المشروع.

## G2 — قاعدة البيانات

النطاق: `Organizations → Branches → Classes → Users` عبر ترحيلات PostgreSQL حتمية، مع القيود والفهارس وRLS الأساسية وبيانات اختبار آمنة.

معيار الخروج: نجاح اختبارات العلاقات والعزل بين مؤسستين وفرعين، ورفض السجلات غير المتسقة من قاعدة البيانات.

## G3 — Authentication / Roles

النطاق: Supabase Auth، الجلسات، الأدوار المعتمدة، ربط الملفات، ودخول الطالب إذا اعتمد PIN.

معيار الخروج: نجاح الدخول والخروج واستعادة الجلسة، واختبارات الدور الافتراضي الآمن ومنع انتحال الدور من العميل.

## G4 — إدارة الفروع والمستخدمين

النطاق: CRUD المصرح به للمؤسسات والفروع والصفوف والطلاب والموظفين وعلاقات المعلمين بالصفوف.

معيار الخروج: نجاح السيناريو الإداري الأساسي، ورفض مدير دمشق الوصول إلى حلب على مستوى قاعدة البيانات.

## G5 — Dhikr / Wird

النطاق: مكتبة الأذكار المعتمدة، تعريف الأوراد، ونطاقات التكليف المطبعة بصف مستقل لكل نطاق.

معيار الخروج: إنشاء ذكر معتمد وورد وتكليف صحيح، مع منع النطاقات المختلطة أو العابرة للمؤسسات.

## G6 — العداد Offline First

النطاق: عداد فوري يحفظ محليًا أولًا ويستعيد التقدم بعد إغلاق التطبيق وإعادة تشغيله.

معيار الخروج: نجاح العد دون شبكة، وعدم فقد التقدم أو تجاوزه الهدف، ونجاح اختبار إعادة التشغيل.

## G7 — المزامنة

النطاق: `sync_events`, وطابور المزامنة، و`event_uuid`، والتحديث الذري للتقدم المجمع.

معيار الخروج: نجاح idempotency وإعادة المحاولة بعد فقد الاستجابة وعدم العد المزدوج أو فقد حدث صحيح.

## G8 — Dashboard

النطاق: مؤشرات الطفل والموظف ومدير الفرع والإدارة المركزية وفق الصلاحيات.

معيار الخروج: تطابق مؤشرات الفرع والمركزية مع البيانات المصدر وعدم الاعتماد على العدد الخام للتصنيف.

## G9 — Points / Badges

النطاق: سجل نقاط قائم على الإكمال والالتزام، وقواعد أوسمة قابلة للإدارة ومنح قابل لإعادة المحاولة.

معيار الخروج: نجاح قواعد النقاط والاستمرارية ومنع المنح المكرر وعدم إظهار ترتيب سلبي للأطفال.

## G10 — Reports

النطاق: تقرير يومي، وتقرير فرع، وتقرير مستخدم، مع الفلاتر الإدارية المعتمدة.

معيار الخروج: صحة النتائج مع تركيبات الفلاتر، وعزل المؤسسة والفرع، وتطابق التقرير مع التقدم المصدر.

## G11 — Security Review

النطاق: مراجعة RLS وRPC والأسرار والخصوصية وسجل التدقيق وإساءة الاستخدام والمزامنة.

معيار الخروج: نجاح اختبارات الاختراق الوظيفية بحسابات فعلية دون `service_role`، وإغلاق الملاحظات الحرجة والعالية.

## G12 — Android Release

النطاق: إعداد توقيع Android، بناء الإصدار، إعداد البيئة، واختبار التثبيت والترقية والمسار الأساسي على جهاز حقيقي.

معيار الخروج: إنتاج APK موقع قابل للتثبيت، مع نجاح سيناريو قبول MVP الحي كاملًا وعدم تضمين أسرار أو إعدادات اختبار.

## الناتج النهائي

بعد اجتياز G0 حتى G12 يكون الناتج **APK قابلًا للتثبيت**، ولا يسمى الإصدار MVP ناجحًا قبل نجاح بوابة القبول الحية المحددة في `MVP_ACCEPTANCE.md`.
