# Student Authentication Policy — Rawdat Wird

الحالة: **OWNER-APPROVED PRODUCT BASELINE — TECHNICAL LOCKOUT DETAILS DEFERRED TO G3**  
لا تتضمن هذه الوثيقة تنفيذ Supabase Auth أو Edge Functions أو SQL.

## 1. اتجاه الدخول المعتمد مقترحًا

`organization_code + student_code + PIN`

- `organization_code` يحل المؤسسة.
- `student_code` فريد داخل المؤسسة، لا عالميًا.
- المقارنة بالرموز غير حساسة لحالة الأحرف بعد التطبيع، وتمنع المسافات المحيطة.
- طول/صيغة PIN ومعاملات الحماية تفاصيل تقنية مؤجلة إلى G3؛ التوصية السابقة كانت 6 أرقام لكنها ليست قيمة مالك نهائية في G0. لا يخزن أو يسجل PIN كنص صريح، ولا يرسله العميل بعد إنشاء الجلسة.
- QR مؤجل إلى POST-MVP، وإذا أضيف لاحقًا يختار المؤسسة/الطالب فقط ثم يظل PIN مطلوبًا. QR وحده لا يصادق طفلًا.

## 2. سياسة المحاولات والقفل

| البند | القرار المقترح |
|---|---|
| المحاولات الفاشلة | يلزم حد خادمي؛ العدد النهائي يحدد في G3 |
| القفل | يلزم قفل مؤقت/إبطاء خادمي؛ المدة النهائية تحدد في G3 |
| الرسالة | عامة؛ لا تكشف هل المؤسسة أو الطالب أو PIN هو الخاطئ |
| العد | خادمي، لا يمكن تصفيره بحذف بيانات التطبيق |
| النجاح | يصفر عداد الفشل المسموح وفق السياسة الخادمية |
| الاستعادة | مدير الفرع أو `admin` المخول يعيد PIN بعد تحقق إداري خارج التطبيق؛ مدير المؤسسة يستطيع ذلك داخل مؤسسته |
| التدقيق | تسجل عمليات إعادة الضبط والقفل دون تسجيل PIN |

لا يعتمد القفل على عنوان IP وحده، ولا يتحول إلى قفل دائم تلقائي للطفل.

## 3. الجلسة والجهاز المشترك

- يصدر مسار خلفي موثوق جلسة Supabase مرتبطة بـ`profiles.id` الخاص بالطالب؛ لا يمنح العميل دورًا أو tenant من مدخلاته.
- مدة الجلسة وآلية التجديد والإبطال تحدد في G3 وفق أقل صلاحية ومخاطر الجهاز المشترك؛ لا تعتمد أرقام تشغيلية نهائية في G0.
- يظهر خروج واضح وسهل في الجهاز المشترك. بعد تسجيل الخروج يحذف التطبيق رموز الجلسة، بيانات الطالب المحلية القابلة للقراءة، عداده ونسخه المؤقتة بعد ضمان عدم إسقاط تقدم معلق.
- إذا بقي تقدم غير متزامن عند الخروج، يجب إتمام مزامنته أو حفظه في حاوية محلية مشفرة معزولة لا يراها الحساب التالي، مع واجهة تحذر المسؤول؛ لا يجوز حذفه بصمت.
- لا يستعيد حساب جديد بيانات حساب الطفل السابق، حتى على الجهاز نفسه.

## 4. الحد الأدنى للخصوصية

1. لا يعرض تسجيل الدخول الاسم الكامل قبل نجاح المصادقة.
2. لا تجمع صورة أو موقعًا أو معرفًا إعلانيًا أو بريد طفل أو رقم هاتفه.
3. لا تظهر رموز الطلاب أو PIN في السجلات والتحليلات وتقارير الأعطال.
4. تحد نتائج البحث/الحل من تعداد الحسابات، برسائل موحدة ومعدل طلبات محدود.
5. يخزن PIN بخوارزمية اشتقاق كلمات مرور بطيئة ومع salt فريد؛ اختيار الخوارزمية والمعلمات قرار أمني في G3/G11.
6. إعادة PIN تُبطل جلسات الطالب القائمة أو تعرض للمسؤول خيار إبطالها مع افتراضي آمن بالإبطال.
7. بيانات Drift معزولة حسب `profile_id`، وتمسح/تعزل عند الخروج المشترك.

## 5. ما يؤجل إلى G3

- خدمة المصادقة الموثوقة وطريقة إصدار/تبادل جلسة Supabase.
- خوارزمية hash ومعلماتها، rate limiting الموزع، refresh token rotation، والتخزين الآمن بحسب Android.
- رحلة إنشاء PIN وتسليمه الأولي وإجبار تغييره إذا لزم.
- تصميم شاشة الدخول والنصوص الدقيقة، مع الالتزام بعدم كشف وجود الحساب.

يجب أن يثبت G3 أن `auth.uid()` الناتج يرتبط بالطالب الصحيح وأن تغيير مدخلات العميل لا ينتحل مؤسسة أو دورًا أو طالبًا آخر.
