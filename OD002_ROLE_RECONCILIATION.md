# OD-002 Role Reconciliation

الحالة: **APPROVED REVISED — G0-OD-03**  
المهمة: `G0-OD-02`  
اعتمد المالك المصالحة في G0-OD-03 دون توسيع صلاحيات `branch_manager`.

## 1. التناقض الأصلي

في `ROLE_AND_SCOPE_MODEL.md` كانت خلية «إدارة المستخدمين» تمنح `branch_manager` الصلاحية `M(B عدا org admin)`. يمكن قراءة ذلك على أنه يسمح له بإنشاء أو تعديل أو تعطيل `branch_manager` آخر داخل الفرع. لكن قسم Provisioning في الوثيقة نفسها يقصر تغيير `branch_manager` و`organization_admin` على `organization_admin`.

كان الوصف العام «المسؤول الكامل عن فرع واحد» أوسع من اللازم، ولم يميز بوضوح إدارة البيانات التشغيلية عن تعيين الأدوار الإدارية العليا.

## 2. الوثائق المتأثرة

- `ROLE_AND_SCOPE_MODEL.md`: الوصف، خلية إدارة المستخدمين، وقواعد النطاق.
- `MVP_SCOPE_BASELINE.md`: حد ميزة Branch manager.
- `GATE_ACCEPTANCE_MATRIX.md`: دليل التحقق في G4.
- `STUDENT_AUTH_POLICY.md`: لا يحتاج تعديلًا؛ إعادة PIN لطالب داخل الفرع سلطة تشغيلية قائمة.
- `LOGICAL_DATA_MODEL.md`: لا يحتاج تعديلًا؛ يحدد النطاق الإلزامي ولا يمنح صلاحية عابرة للفروع.
- `PRODUCT_REQUIREMENTS.md` و`DATABASE_SCHEMA.md`: لم يعدلا في هذه المهمة؛ نصوصهما العامة لا تتقدم على المقترح المصحح قبل موافقة المالك.

## 3. الصلاحية المصححة المقترحة

1. `branch_manager` يحمل `organization_id + branch_id` إلزاميين ويعمل داخل هذا الفرع فقط.
2. يستطيع إدارة البيانات التشغيلية اللازمة للـMVP داخل فرعه: الصفوف، الطلاب، المعلمون، الموظفون، `admin` التشغيلي، الأوراد، التكليفات، التقارير، والمراجعات الفرعية المصرح بها.
3. لا يستطيع إنشاء أو تعديل أو تعطيل أو تعيين `branch_manager` أو`organization_admin`.
4. لا يستطيع إنشاء فرع شقيق أو قراءة بياناته أو إدارته، حتى إذا عرف معرفاته الصحيحة.
5. لا يستطيع نقل مستخدم أو صف أو سجل إلى مؤسسة أو فرع آخر.
6. تبقى إعدادات المؤسسة، العمليات العابرة للفروع، وتعيين الأدوار الإدارية العليا لدى `organization_admin`.
7. لا تمنح هذه المصالحة صلاحيات جديدة خارج احتياجات MVP؛ هي تضيق الوصف السابق وتزيل الالتباس.

## 4. التغييرات الدقيقة المنفذة في المقترحات

### `ROLE_AND_SCOPE_MODEL.md`

- استبدال «المسؤول الكامل» بوصف إدارة البيانات التشغيلية لفرع واحد.
- تغيير خلية إدارة المستخدمين إلى:
  `M(B للطالب/teacher/staff/admin؛ بلا تعيين أو تغيير branch_manager/org admin)`.
- إضافة منع صريح للوصول إلى sibling branches ونقل tenant وتعيين الأدوار العليا.
- تأكيد أن RLS وRPC يفرضان الحدود وأن UI ليس حدًا أمنيًا.

### `MVP_SCOPE_BASELINE.md`

- تقييد Branch manager بالبيانات التشغيلية لفرعه وبالمستخدمين العاديين.
- إضافة منع صريح للوصول إلى فرع شقيق وتعيين الأدوار الإدارية العليا.

### `GATE_ACCEPTANCE_MATRIX.md`

- إضافة دليل G4 سلبي يثبت أن `branch_manager` لا يقرأ/يدير فرعًا شقيقًا ولا يعيّن/يغير `branch_manager` أو`organization_admin`.

## 5. تأكيد عزل tenant والفرع

العزل محفوظ ومشدد:

- كل عملية تحمل/تستنتج المؤسسة والفرع من Profile الخادمي الموثوق.
- لا تقبل RLS/RPC `organization_id`, `branch_id`, أوrole المرسلة من العميل دليلًا للصلاحية.
- يجب أن يفشل مدير دمشق عند استدعاء REST أو RPC لفرع حلب مباشرة.
- يجب اختبار SELECT/INSERT/UPDATE/DELETE وRPC، لا إخفاء عناصر الواجهة فقط.
- `organization_admin` يظل مقيدًا بمؤسسته حتى عند العمل عبر فروعها.

## 6. مقترح OD-002 المصحح للمالك

**السؤال المعاد:** هل يعتمد المالك الأدوار الستة ومصفوفة `ROLE_AND_SCOPE_MODEL.md` بعد تقييد `branch_manager` بإدارة البيانات التشغيلية لفرعه فقط، ومنعه من الفروع الشقيقة ومن تعيين أو تغيير `branch_manager` و`organization_admin`، مع بقاء السلطات المؤسسية لدى `organization_admin` وفرض الحدود خادميًا؟

**التوصية:** `APPROVE CORRECTED OD-002`.

**قرار المالك:** `APPROVE REVISED — G0-OD-03`.

`OD-002 = APPROVED`.
