# المعمارية التقنية

> **إشعار سلطة G0:** هذه الوثيقة تحتوي تفاصيل prototype وأسماء جداول/RPC قديمة. تحكم المعمارية المستهدفة وثائق G0 المعتمدة، وبخاصة `LOGICAL_DATA_MODEL.md`, `ROLE_AND_SCOPE_MODEL.md`, و`OFFLINE_SYNC_CONTRACT.md`. مدة grace window مؤجلة إلى G7، وقيم النقاط إلى G9، ولا يجوز أي انتقال هدّام ما دام غياب حالة الإنتاج `NOT VERIFIED`.

## 1. النطاق والتقنية

هذه معمارية تطبيق Flutter يعمل على Android وiOS. يعتمد التطبيق:

- `flutter_riverpod` لإدارة الحالة والاعتماديات.
- `go_router` للتنقل وحماية المسارات.
- `supabase_flutter` للمصادقة والاتصال بخدمات Supabase.
- `drift` للتخزين المحلي والعمل دون اتصال.
- `connectivity_plus` كإشارة مساعدة لإعادة المزامنة.
- `freezed` للنماذج وحالات الواجهة غير القابلة للتغيير.
- `json_serializable` للتحويل الصريح بين DTOs وJSON.

## 2. المبادئ

- الواجهة تقرأ من Drift كي تتصرف بصورة ثابتة مع الشبكة ومن دونها.
- Supabase هو المصدر النهائي للبيانات المتزامنة، وتفرض PostgreSQL القيود والصلاحيات.
- الكتابة محلية أولًا، ثم تدخل طابور مزامنة قابلًا لإعادة المحاولة بأمان.
- قواعد العمل مستقلة عن Widgets وعن تفاصيل مصدر البيانات.
- تدير ميزة `users` سجلات مستخدمي الفروع الأربعة، بينما تتحقق قاعدة البيانات من توافق الفرع والصف.
- تدير ميزة `dhikr` العداد محليًا أولًا؛ كل زيادة تحدث جلسة Drift وتضيف عملية مزامنة في معاملة واحدة.
- تدير ميزة `wirds` أوراد الفروع البعيدة عبر حالات `draft`, `assigned`, و`completed`، وتفرض PostgreSQL الانتقالات وتوافق فرع المكلّف.
- تدير ميزة `child_journey` ورد الطفل ومكافآته عبر RLS، بينما يعيد عداد Drift استخدام طابور العمليات مع `managed_wird_id` اختياري.
- تدير ميزة `employee_journey` سياق الموظف والورد والتقدم والسجل، وتستخدم نوع `branch_users` لاختيار Home المناسبة.
- يعمل `can_view_linked_class` بوصفه حد تفويض الخادم للمعلم المرتبط بصف، دون الاعتماد على إخفاء الواجهة.
- تدير ميزة `branch_dashboard` مؤشرات اليوم، وترسل حدود اليوم المحلي كتوقيتين مطلقين إلى RPC لتجنب افتراض منطقة زمنية في الخادم.
- تدير ميزة `central_dashboard` مقارنة الفروع داخل المؤسسة؛ تحسب RPC مقامي المشاركة والإكمال وتعيد الفروع المسموح بها فقط، ثم يثبت نطاق المجال ترتيب الإكمال فالمشاركة.
- تدير ميزة `dhikr_library` تعريفات الأذكار ودورة مراجعتها؛ تفرض قاعدة البيانات المصدر والمراجع للاعتماد، وتبطل الاعتماد السابق عند تغير المحتوى.
- تدير ميزة `wird_programs` إنشاء الورد الجماعي. تنفذ RPC واحدة إنشاء الورد ونطاقاته وتكليفات الجمهور في معاملة ذرية، وتحفظ لقطات الذكر والهدف في `managed_wirds`.
- يمثل `WirdAssignmentScope` اتحادًا مقيدًا لستة نطاقات؛ تعرض الواجهة حقول النوع المختار فقط، وتعيد `create_wird_program_v2` التحقق من شكل النطاق وملكيته على الخادم.
- تدير ميزة `campaigns` الهدف الجماعي والتقارير. تقفل `apply_campaign_increment` صف الحملة، وتسجل mutation ذات معرف فريد، ثم تحدث الإجمالي ذريًا؛ ويعيد `get_campaign_dashboard` تجميع لقطات الفرع والفئة.
- يفصل المحتوى الديني المراجع عن المحتوى الذي ينشئه المستخدم.
- يعتمد اليوم على المنطقة الزمنية للمستخدم.

## 3. المكونات

```text
تطبيق Flutter
  ├─ Widgets عربية RTL
  ├─ GoRouter
  ├─ Riverpod Notifiers / Providers
  ├─ حالات ونماذج Freezed
  └─ Repositories
       ├─ Drift ─────► قاعدة محلية + طابور مزامنة
       └─ supabase_flutter
              │ HTTPS
              ▼
        Supabase
          ├─ Auth
          ├─ PostgreSQL + RLS
          ├─ RPC / Edge Functions عند الحاجة
          └─ مهام مجدولة ──► مزود الإشعارات/البريد
```

## 4. حدود الطبقات

### العرض والتنقل

- Widgets للمصادقة والإعداد وورد اليوم والسجل والإعدادات.
- يدير `go_router` المسارات وإعادة التوجيه بناءً على حالة المصادقة المتاحة عبر Riverpod.
- تدعم المكونات RTL وحالات التحميل والخطأ والفراغ والمزامنة المعلقة.

### الحالة وحالات الاستخدام

- تدير Riverpod الحالة والاعتماديات، من دون وضع قواعد العمل داخل Widgets.
- تنسق حالات الاستخدام إنشاء الخطة وتسجيل التقدم وتشغيل المزامنة.
- تمثل Freezed حالات التحميل والبيانات والخطأ والنماذج غير القابلة للتغيير.

### النطاق

- يحوي قواعد التكرار واحتساب اليوم وحدود التقدم والأرشفة.
- لا يستورد Flutter أو Drift أو Supabase حيثما كان الفصل عمليًا.

### مستودعات البيانات

- تخفي Repository واجهتي Drift وSupabase عن طبقة العرض.
- Drift هو مصدر الـstreams التي تراقبها الواجهة، وتتم الكتابة المحلية داخل معاملات.
- يحول `json_serializable` صفوف Supabase إلى DTOs صريحة، ثم تحول DTOs إلى نماذج النطاق.
- تمر تغييرات Drift عبر `schemaVersion` واختبارات ترحيل، وتغييرات Supabase عبر ملفات SQL في `supabase/migrations`.

## 5. بنية المشروع

```text
lib/
  app/                         # MaterialApp.router والثيم والتعريب
  routing/                     # GoRouter وحراس المسارات
  core/                        # الأخطاء والوقت والاتصال والأدوات المشتركة
  data/
    local/                     # جداول Drift وDAOs والترحيلات
    remote/                    # Supabase DTOs ومصادر البيانات
    sync/                      # الطابور والمنسق وسياسات التعارض
  features/
    auth/
    organizations/                # المؤسسات والفروع والصفوف والأطفال
    plans/
    daily_wird/
    reminders/
    content/
  l10n/
supabase/migrations/
test/
integration_test/
```

داخل كل ميزة تُفصل طبقات `presentation`, `domain`, و`data` عند الحاجة، من دون إنشاء ملفات شكلية لا تقدم حدًا حقيقيًا.

## 6. تدفق ورد اليوم

1. يحسب التطبيق التاريخ المحلي وفق المنطقة الزمنية المحفوظة.
2. يقرأ Repository ورد اليوم من Drift وتعرضه الواجهة فورًا.
3. ينشئ العناصر المحلية الناقصة بمعاملة وقيد فريد.
4. عند توفر الشبكة، يدفع منسق المزامنة العمليات المعلقة إلى Supabase.
5. تفرض RLS ملكية البيانات، ثم يسحب التطبيق التغييرات البعيدة إلى Drift.
6. تبث Drift الحالة النهائية إلى Riverpod والواجهة.

لا يعتمد تحديد اليوم على توقيت خادم Supabase وحده، وتُختبر حدود منتصف الليل في مناطق زمنية متعددة.

## 7. استراتيجية المزامنة

- يسجل كل تعديل محلي عملية في `sync_queue` بمعرف UUID ثابت.
- يحاول Sync Coordinator دفع العمليات بالترتيب، ثم سحب التغييرات البعيدة وتطبيقها داخل معاملة Drift.
- `connectivity_plus` يوقظ المنسق عند تغير الاتصال، لكنه لا يثبت وجود الإنترنت؛ نجاح الطلب نفسه هو الحكم.
- تمنع القيود الفريدة ومعرف العملية تطبيق العملية ذاتها مرتين.
- تستخدم البيانات القابلة للحذف `deleted_at` كي تنتقل عمليات الحذف بين الأجهزة.
- تستخدم إعادة المحاولة تأخيرًا متزايدًا، وتظل العمليات الفاشلة ظاهرة للمستخدم عند الحاجة.

### تسجيل التقدم

1. يحدث التطبيق `daily_items` ويضيف mutation إلى `sync_queue` في معاملة محلية واحدة.
2. تظهر النتيجة فورًا حتى دون اتصال.
3. عند الدفع، تستدعي طبقة Supabase عملية RPC ذرية تتحقق من الهوية والحدود وعدم تكرار `operation_id`.
4. لا تستبدل زيادة التقدم بقيمة قديمة؛ تعالج RPC العملية وفق دلالتها لا بوصفها كتابة صف عمياء.
5. بعد النجاح، تزال العملية من الطابور وتحدث Drift بالقيمة المؤكدة من الخادم.

### دورة حياة العداد دون اتصال

تنفذ `incrementDhikr` تحديث `dhikr_sessions` وإضافة صف `dhikr_mutations` في معاملة SQLite واحدة، ثم تعيد الحالة من Drift إلى Riverpod والواجهة. لذلك إما أن تحفظ الضغطة والعملية معًا أو لا يحفظ أي منهما عند انقطاع مفاجئ.

عند إنشاء `DhikrController` بعد استعادة Session، يحمل آخر جلسة خاصة بالحساب من Drift. إذا وجد عمليات غير متزامنة، يبدأ محاولة دفع مباشرة، إضافة إلى الاستماع لتغيرات `connectivity_plus`. لا تمثل إشارة الاتصال نجاحًا؛ تبقى العملية معلقة حتى تنجح RPC. عند فقد الرد بعد تنفيذ الخادم، تعاد العملية بالـ`operation_id` نفسه فتمنع قيود Supabase الزيادة المزدوجة.

ملف SQLite دائم على الجهاز وليس ذاكرة مؤقتة؛ إغلاق التطبيق أو إعادة تشغيل الهاتف لا يحذف الجلسات أو العمليات. يغلق Provider الاتصال بقاعدة البيانات عند التخلص منه دون حذف الملف.

### مؤشرات النزاهة غير العقابية

تراقب Triggers خادمية البيانات التشغيلية الموجودة أصلًا في `managed_wird_mutations` ولا تضيف مراقبة جهاز أو موقع أو محتوى. تقرأ العتبات المفعلة من `integrity_rules` وتنشئ صفًا في `integrity_flags` عند النشاط السريع أو المرتفع. يسجل `managed_wird_sync_attempts` عدد محاولات `operation_id` نفسه لاكتشاف خلل مزامنة محتمل.

### التقارير الإدارية المفلترة

تدير ميزة `reports` مسودة الفلاتر وعرض نتائج الإنجاز، بينما تنفذ RPC `get_administrative_completion_report` التصفية الفعلية داخل PostgreSQL بعد التحقق من `can_manage_organization`. تقبل الفترة والفرع والصف والدور والمستخدم والورد والذكر وحالة الإتمام في استدعاء واحد، وتعيد فقط تكليفات المؤسسة المصرح بها. تعيد RPC مستقلة خيارات الفلاتر مع علاقات الفرع والصف والمستخدم حتى تمنع الواجهة التركيبات غير المتسقة قبل الإرسال.

تُفسر الفترة حسب المنطقة الزمنية المحفوظة مع التكليف، أو `Asia/Damascus` للتكليفات القديمة، ويكون تاريخا البداية والنهاية شاملين. لا تتجاوز نافذة الطلب 366 يومًا لحماية كلفة التقرير.

يستهلك MVP الصفوف المفلترة في ثلاثة عروض فقط: تجميع حسب اليوم، وملخص فرع محدد، وسجل مستخدم محدد. يمنع العرضان الأخيران التشغيل دون معرف صريح حتى يبقيا تقريرين مفردين، بينما تؤجل المقارنات والتجميع الأسبوعي والشهري والتصدير إلى مرحلة لاحقة.

### بوابة قبول MVP

يجمع اختبار `test/acceptance/mvp_success_scenario_test.dart` مسار Drift الفعلي وطابور المزامنة مع عقد خلفية اختباري، وينفذ 100 عملية ذات معرفات فريدة ثم يتحقق من اكتمال الورد وتطابق ملخصي الفرع والإدارة المركزية. تكمل قائمة `MVP_ACCEPTANCE.md` هذا الاختبار بتشغيل حي على Supabase للتحقق من حدود لا تستطيع المحاكاة إثباتها: ربط `auth.uid()`, وسياسات RLS، وRPC، وTriggers.

### حدود الوصول الخادمية

تتعامل المعمارية مع Supabase RLS بوصفه حد الأمان النهائي. يستخرج الخادم هوية المتصل من JWT ويربطها بـ`profiles` أو بجلسة طالب موثوقة؛ لا يقبل المؤسسة أو الفرع أو الدور المرسل من Flutter دليلًا للصلاحية. يقتصر الطالب على نفسه، والمعلم على علاقات `teacher_classes`، ومدير الفرع على `profiles.branch_id`، ومدير المؤسسة على `profiles.organization_id`. تتحقق RPC من النطاق نفسه قبل أي استعلام، وتغطي الاختبارات السلبية REST وRPC بحسابات فعلية دون `service_role`.

كل كاشف محاط بمعالجة خطأ غير مانعة؛ فشل الرصد يعيد العملية الأصلية كما هي. لا توجد أي علاقة حذف أو تعديل بين العلامات وبين `managed_wirds`, التقدم، النقاط أو الأوسمة. تعرض `get_branch_integrity_flags` العلامات لمدير الفرع فقط، بينما تحفظ `review_integrity_flag` نتيجة المراجعة أو الاستبعاد دون حذف العلامة. الأدلة تقتصر على العدد والنافذة الزمنية.

### نقاط الالتزام

عند انتقال الورد من `assigned` إلى `completed` تستدعي RPC محرك النقاط داخل المعاملة نفسها. يقرأ المحرك القواعد المفعلة من `point_rules`، ويسجل كل استحقاق في `point_awards` بمفتاح فريد ولقطة للنقاط. يسجل `user_compliance_days` الأيام التي اكتملت فيها جميع أوراد الطفل المستحقة، ويشتق منها الاستمرارية ومكافآت 3 و7 و30 يومًا. لا يؤدي تعديل قاعدة لاحقًا إلى تعديل الجوائز التاريخية، ولا تمنح أي نقطة مقابل الضغطة المنفردة.

يظل `child_reward_summaries` ملخص قراءة سريعًا للشاشة، ويعاد بناؤه من سجل الاستحقاقات عند كل إتمام ناجح. يحتسب اليوم في منطقة الورد الزمنية، وتبقى شاشة الطفل بثلاثة أقسام فقط عبر عرض الملخص داخل «مكافآتي».

### الأوسمة

تخزن `badge_definitions` تعريف الوسام ونوع منحه وشرط الاستمرارية، بينما تمثل `badge_awards` دفتر الأوسمة المكتسبة بقيد فريد لكل طفل ووسام. يمنح Trigger الأوسمة التلقائية بعد تحديث ملخص الالتزام، وتنفذ `award_manual_badge` منح الوسام المتقدم بعد التحقق من صلاحية مدير الفرع. لا يكتب تطبيق الطفل أي وسام، ويقرأ فقط أوسمته عبر RLS.

### سياسة تصنيف الأطفال

لا يحتوي نموذج حالة الطفل أو مستودعه على حقل ترتيب، وتظل قراءات `child_reward_summaries`, `user_compliance_days`, و`badge_awards` مقيدة بربط `profile_id = auth.uid()`. لذلك لا يستطيع الطفل استنتاج موقعه من بيانات بقية الأطفال حتى لو عُدلت الواجهة.

تستخدم الإدارة `get_branch_child_rewards` بوصفها حدود قراءة منفصلة تتحقق من `can_manage_branch`. تعيد الإنجاز والنقاط والاستمرارية والأوسمة لكل أطفال الفرع، مرتبة بالاسم فقط. لا تعيد RPC رقم ترتيب أو نسبة مئينية أو مقارنة وصفية، وتستهلكها لوحة Rewards الإدارية وحدها.

سياسة التعارض لتعديلات العنوان والترتيب والحذف تحتاج اعتمادًا قبل التنفيذ؛ لا يستخدم "آخر كاتب يفوز" ضمنيًا.

## 8. التذكيرات

- تخزن الساعة المحلية والمنطقة الزمنية بدل موعد UTC ثابت دائمًا.
- يمكن تنفيذ التذكير المحلي على الجهاز أو عبر مهمة Supabase ومزود خارجي بعد اعتماد القناة.
- يسجل الإرسال البعيد بقيد فريد لكل تذكير ويوم لمنع التكرار.
- تعالج تغييرات التوقيت الصيفي عبر مكتبة مناطق زمنية موثوقة.

حزمة الإشعارات نفسها لم تُعتمد بعد، لذلك لا تفترض المعمارية حزمة Flutter إضافية.

## 9. المصادقة والصلاحيات

- تدير Supabase Auth الجلسة عبر `supabase_flutter` والتخزين الآمن الذي تدعمه المنصة.
- يراقب `AuthController` أحداث Session ويعرض حالة تحميل حتى يقرأ الدور من `profiles`.
- الأدوار المعتمدة في المرحلة الثانية هي `member` و`admin`، والدور الافتراضي للحساب الجديد هو `member`.
- يحول GoRouter المستخدم العادي إلى `/home` والمدير إلى `/admin`، ويرفض الدور المفقود أو غير المعروف.
- تفعّل RLS على كل جدول خاص وتستخدم `auth.uid()` لفرض الملكية.
- لا يُقبل `user_id` من العميل بوصفه دليل ملكية، حتى إن احتواه الصف.
- مفتاح Supabase العام فقط مسموح داخل التطبيق؛ مفتاح `service_role` لا يُضمّن مطلقًا.
- لا تمنح قاعدة البيانات تطبيق Flutter صلاحية تحديث `profiles.role`؛ تغيير الدور عملية إدارية موثوقة.
- تحمى Edge Functions والمهام المجدولة بهوية مستخدم أو خدمة مناسبة.

## 10. إدارة المؤسسات

- تمثل `Organization → Branch → Class → Child` حدود الملكية والتنقل في لوحة المدير.
- يخفي `OrganizationsRepository` تفاصيل Supabase عن Riverpod والواجهة.
- يحمل `OrganizationManagementController` المستوى المحدد وما تحته، ويعيد تحميل الأبناء بعد كل mutation.
- ينشئ Trigger عضوية `owner` عند إنشاء المؤسسة، وتفحص دوال RLS العضوية عبر كامل سلسلة الانتماء.
- CRUD الإداري متصل مباشرة بـSupabase في المرحلتين الثالثة والرابعة لتجنب تعارضات إدارية صامتة؛ لا يدخل Drift قبل اعتماد سياسة مزامنة لهذه البيانات.
- الحذف ناعم عبر `deleted_at`، وتستبعد الاستعلامات الصفوف المحذوفة.

## 11. الأداء والاحتفاظ المحلي

- فهارس بعيدة ومحلية على الملكية والتاريخ وحالة النشاط.
- تقسيم السجل إلى صفحات بدل تحميل جميع الأيام.
- تحتفظ Drift بالحد الأدنى اللازم من السجل والمحتوى المراجع للعمل دون اتصال.
- تنظف عمليات المزامنة المؤكدة وفق سياسة احتفاظ، ولا تنظف العمليات المعلقة بسبب فحص اتصال فقط.

## 12. المراقبة والأخطاء

- أخطاء نطاق معروفة وقابلة للترجمة بدل تمرير استثناءات Supabase أو Drift مباشرة إلى الواجهة.
- سجلات منقحة لا تحتوي أسرارًا أو محتوى مستخدم أو نصوصًا دينية مخصصة.
- قياسات لأخطاء الحفظ وحجم الطابور وعمر أقدم عملية ومعدل نجاح التذكيرات.
- لا تسجل `access_token`, `refresh_token`, أو حمولة `service_role` مطلقًا.

## 13. الاختبارات

- اختبارات وحدات لقواعد التكرار والمناطق الزمنية ونسبة الإنجاز.
- اختبارات Riverpod لحالات الاستخدام من دون الاعتماد على Widgets.
- اختبارات Drift للمعاملات والترحيلات والقيود الفريدة.
- اختبارات Supabase محلية لسياسات RLS وRPC ومنع تكرار العملية.
- اختبار تطبيقي لإنشاء المؤسسة وفرع دمشق والصف والطفل والتحقق من المفاتيح الخارجية المنطقية.
- اختبارات لوصول الأنواع الأربعة وقاعدة إلزام الصف للطفل والمعلم ومنعه للإداري ومدير الفرع.
- اختبارات Drift لاستمرار العداد، وحد الاكتمال، وعزل الحسابات، وبقاء العملية عند فشل الشبكة.
- اختبار إعادة إرسال `operation_id` نفسه بعد فقد الرد والتأكد من عدم مضاعفة العدد البعيد.
- اختبارات دورة إدارة الورد، ورفض التكليف عبر الفروع، ومنع تغيير الورد المنتهي.
- اختبارات Widget لواجهة الطفل المكلف وحالة الإتمام، واختبار ترحيل Drift من الإصدار 1 إلى 2.
- اختبار احتفاظ العداد المحلي برابط الورد وإرساله إلى بوابة المزامنة.
- اختبارات Widget لأقسام الموظف، والسجل، وظهور «صفي» للمعلم المرتبط واختفائه عن الإداري.
- اختبار تحويل مؤشرات RPC واختبار Widget للمؤشرات الستة ومداخل لوحة الفرع.
- اختبارات تحويل مؤشرات الإدارة المركزية، وترتيب الفروع دون الاعتماد على العدد الخام، وفلتر كل الفروع/فرع واحد.
- اختبارات تحويل تعريف الذكر وعرض النص والهدف والحالة والمصدر في واجهة المكتبة.
- اختبارات إعادة استخدام الذكر، وحدود مدة التكليف، وعرض حقول إنشاء الورد والجمهور كاملة.
- اختبار Widget يثبت ظهور أنواع النطاق الستة وتحول النموذج إلى حقول الدور عند اختياره.
- اختبارات تحويل بيانات الحملة وعرض الإجمالي المنسق وتفصيل الفروع والفئات.
- اختبار Widget لعرض مجموع نقاط الالتزام والاستمرارية الحالية وأفضل استمرارية.
- اختبار Widget لعرض الأوسمة المكتسبة داخل «مكافآتي».
- اختبار عدم ظهور عبارات المركز والترتيب في واجهة الطفل، واختبار عرض التقرير الكامل في واجهة الإدارة دون حقل ترتيب.
- اختبار قاعدة بيانات فعلية بملف مؤقت يثبت بقاء العدد والطابور عبر إغلاق اتصال Drift وفتحه مرتين، ثم مزامنة كل ضغطة مرة واحدة.
- اختبار Controller يثبت بدء مزامنة الطابور المستعاد فور تشغيله حتى دون حدث اتصال جديد.
- اختبار قبول رقمي يبدأ بمزامنة 20/100، يضيف 30 عملية أثناء الانقطاع، يغلق ملف SQLite ويفتحه ليتحقق من 50/100 و30 عملية معلقة، ثم يثبت وصول العداد البعيد إلى 50 وإفراغ الطابور.
- اختبار تحويل وعرض الأنواع الثلاثة لمؤشرات النزاهة، مع نص صريح يؤكد أنها لا تحذف الإنجاز ولا تغير المكافآت.
- اختبارات تكامل لوضع الطيران وإغلاق التطبيق ثم استئناف المزامنة.
- اختبارات Widgets ووصول للشاشات الأساسية واتجاه RTL وGoRouter redirects.

## 14. قرارات معمارية مؤجلة

- مزودات تسجيل الدخول الإضافية؛ المرحلة الثانية تستخدم البريد وكلمة المرور.
- حزمة وقناة التذكيرات: محلية أو Push أو بريد.
- سياسة وضع الضيف وربط صفوف Drift بحساب Supabase بعد التسجيل.
- سياسة التعارض لتعديلات الخطة والحذف المتزامن.
- آلية استيراد النصوص الدينية والتحقق من سلامتها.
- مدة الاحتفاظ المحلي والبعيد بسجلات المزامنة والتذكيرات.
