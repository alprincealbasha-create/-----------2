# قبول الإصدار الأول

> **إشعار سلطة G0:** هذا السيناريو يبقى هدف القبول، لكن أسماء `member`, `branch_users`, و`managed_wird` أدناه هي مصطلحات prototype متجاوزة. يجب تحديث التنفيذ/الاختبار في Gates اللاحقة إلى `profiles` والأدوار الستة و`user_wird_instances/user_wird_progress` والعقد المجمع المعتمد، من دون تغيير نتيجة السيناريو أو عزل RLS.

لا يعد MVP ناجحًا إلا بعد نجاح الاختبار الآلي وقائمة التحقق الحية أدناه كاملة على مشروع Supabase مخصص للاختبار.

## المتطلبات السابقة

- تطبيق جميع ترحيلات `supabase/migrations` بالترتيب.
- وجود حساب مدير بدور `admin` وحساب طفل بدور `member`.
- ربط حساب الطفل بسجل `branch_users.profile_id` من بيئة موثوقة. لا يملك تطبيق Flutter صلاحية كتابة هذا الحقل عمدًا.
- استخدام تعريف ذكر مراجع بشريًا وله مرجع مصدر وحالة `approved`؛ لا يعتمد اختبار القبول على محتوى ديني مولد.
- ضبط الجهاز والمنطقة الزمنية وتاريخ الورد بحيث يكون الورد متاحًا يوم الاختبار.

## السيناريو الإلزامي

| الخطوة | الإجراء | دليل النجاح |
|---|---|---|
| 1 | إنشاء المؤسسة | تظهر في لوحة الإدارة ولا يقرأها مدير غير مخول |
| 2 | إنشاء «فرع دمشق» | يظهر ضمن المؤسسة وفي فلتر الفروع |
| 3 | إنشاء صف | يظهر داخل فرع دمشق فقط |
| 4 | إضافة طفل | يرتبط بالفرع والصف، ثم يربط حسابه من البيئة الموثوقة |
| 5 | إنشاء تعريف ذكر | يمر بالمراجعة البشرية ويصبح `approved` |
| 6 | إنشاء ورد بهدف 100 | يحتفظ بلقطة الذكر والهدف والتاريخ |
| 7 | تكليف الطفل | ينشأ `managed_wird` واحد للطفل دون تكرار |
| 8 | دخول الطفل | ينتقل إلى Home وتظهر بطاقة الورد الحالي |
| 9 | تنفيذ 100 ضغطة | يصل العداد محليًا إلى `100/100` ولا يتجاوز الهدف |
| 10 | حفظ الإنجاز | تصبح العمليات معلّمة كمزامنة ويصبح الورد `completed` على الخادم |
| 11 | مدير الفرع | المشاركون 1، المكتملون 1، إجمالي الأذكار 100، ونسبة الإتمام 100% |
| 12 | الإدارة المركزية | يظهر فرع دمشق بالقيم نفسها ضمن المؤسسة |

## قواعد الفشل

- أي فقد أو تكرار للضغطات يفشل الإصدار.
- ظهور النتيجة في واجهة دون حفظها على الخادم يفشل الإصدار.
- اختلاف نتيجة لوحة الفرع عن الإدارة المركزية يفشل الإصدار.
- نجاح السيناريو بحساب يقرأ مؤسسة غير مصرح بها لا يعد نجاحًا، بل ثغرة صلاحيات.
- لا تكفي اختبارات Flutter المحلية لإعلان النجاح النهائي؛ يجب تنفيذ القائمة الحية بعد تطبيق الترحيلات على Supabase.

## التشغيل الآلي

يشغّل `flutter test test/acceptance/mvp_success_scenario_test.dart` عقد السيناريو محليًا باستخدام Drift ومسار المزامنة الحقيقي، ويتحقق من وصول 100 عملية فريدة إلى ملخصي الفرع والإدارة المركزية. يبقى اختبار Supabase الحي مطلوبًا للتحقق من RPC وRLS وTriggers.
