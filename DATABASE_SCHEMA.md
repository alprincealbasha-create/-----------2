# مخطط قاعدة البيانات

> **إشعار سلطة G0 (G0-OD-03):** `LOGICAL_DATA_MODEL.md` هو النموذج المنطقي المعتمد للـMVP. الجداول والأدوار وRPC القديمة في هذه الوثيقة توثق prototype ولا تعد مصدر حقيقة موازٍ عند التعارض. لا تنفذ migrations من هذه الوثيقة في G0. غياب بيانات الإنتاج `NOT VERIFIED`، لذلك يمنع حذف/reset/overwrite أو افتراض أن أي حالة قائمة فارغة؛ يتطلب الانتقال لاحقًا بيئة معزولة أو قرار migration آمنًا وفق OD-012.

## 1. نظرة عامة

يتكون التخزين من مخطط بعيد في Supabase PostgreSQL ومخطط محلي في Drift. تُنشأ UUID في العميل كي تعمل الكتابة دون اتصال، وتستخدم PostgreSQL أنواع `TIMESTAMPTZ` و`DATE`. يحمل Drift تمثيلًا متوافقًا مع الصفوف البعيدة إضافة إلى بيانات المزامنة المحلية.

كل جدول بعيد خاص بالمستخدم يجب أن يفعّل Row Level Security (RLS)، ولا تكفي فلترة تطبيق Flutter لحماية البيانات.

## 2. العلاقات

```text
auth.users 1 ─── 1 profiles 1 ─── 1 user_preferences
profiles * ─── * organizations (عبر organization_members)
organizations 1 ─── * branches 1 ─── * branch_users
organizations 1 ─── * dhikr_definitions 1 ─── * wirds
wirds 1 ─── * managed_wirds
wirds 1 ─── * wird_assignments
branches 1 ─── * classes 1 ─── * branch_users (للطفل والمعلم)
classes 1 ─── * students * ─── 0..1 profiles
teachers * ─── * classes (عبر teacher_classes)
branches 1 ─── * managed_wirds * ─── 1 branch_users (المكلّف)
branch_users 1 ─── 1 child_reward_summaries
managed_wirds 1 ─── 1 managed_wird_progress 1 ─── * managed_wird_mutations
wirds 1 ─── * user_wird_progress
user_wird_progress 1 ─── * sync_events
organizations 1 ─── * badges 1 ─── * user_badges
organizations 1 ─── * audit_logs
profiles 1 ─── * wird_plans 1 ─── * plan_items
plan_items 1 ─── * daily_items * ─── 1 daily_days
profiles 1 ─── * daily_days
profiles 1 ─── * reminders 1 ─── * reminder_deliveries
profiles 1 ─── * client_mutations
profiles 1 ─── * dhikr_sessions 1 ─── * dhikr_mutations
content_sources 1 ─── * verified_contents
verified_contents 1 ─── * plan_items (اختياري)
```

## 3. الجداول

### قاعدة تطبيع نطاقات التكليف

عند استهداف عدة فروع أو عدة نطاقات، ينشأ صف مستقل في `wird_assignments` لكل نطاق. يمنع تخزين عدة معرفات في عمود واحد، سواء كانت نصًا مفصولًا بفواصل أو مصفوفة أو JSON.

مثال غير مسموح:

```text
branch_ids = "1,2,3"
```

والتمثيل الصحيح:

```text
wird_assignment 1 → branch_id = 1
wird_assignment 2 → branch_id = 2
wird_assignment 3 → branch_id = 3
```

يحافظ ذلك على سلامة المفاتيح الخارجية، ويسهل RLS والفهرسة والتقارير وإضافة النطاقات أو إزالتها دون تحليل نصوص. تنشأ صفوف التكليف المتعددة داخل معاملة واحدة، ويمنع قيد فريد تكرار النطاق نفسه للورد نفسه.

### `auth.users`

تديره Supabase Auth ولا ينشئ التطبيق بديلًا له. المفتاح `id` هو هوية المستخدم، والبريد وبيانات مزود الدخول تبقى تحت إدارة Auth.

### `profiles`

يمثل هذا الجدول امتدادًا مباشرًا لحساب `auth.users` في Supabase، ولا يخزن كلمة المرور أو رموز المصادقة.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي وخارجي إلى `auth.users.id` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `branch_id` | UUID | مفتاح خارجي اختياري إلى `branches.id`؛ يكون `NULL` للمدير المركزي |
| `first_name` | TEXT | الاسم الأول |
| `father_name` | TEXT | اسم الأب |
| `last_name` | TEXT | اسم العائلة |
| `role` | TEXT | دور الحساب؛ قائمة القيم المسموحة لم تعتمد بعد |
| `status` | TEXT | حالة الحساب؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

لأن `organization_id` إلزامي، لا يمكن لـTrigger تسجيل عام إنشاء الملف بأمان من بيانات يرسلها المستخدم بنفسه. يجب إنشاء الحساب والملف ذريًا عبر دعوة أو وظيفة خادمية موثوقة تحدد المؤسسة والدور والفرع، أو اعتماد حالة تجهيز مؤقتة صريحة في تصميم لاحق. لا يستطيع المستخدم تغيير `organization_id`, أو`branch_id`, أو`role`, أو`status` من تطبيق Flutter.

قواعد الاتساق الإلزامية:

- إذا كان `branch_id` غير فارغ، فيجب أن يتبع الفرع `organization_id` نفسه.
- يكون `branch_id IS NULL` مسموحًا للمدير المركزي فقط.
- يجب ألا يستطيع ملف في مؤسسة قراءة ملفات مؤسسة أخرى عبر RLS.
- حذف مستخدم Auth يحذف ملفه وفق سياسة `ON DELETE CASCADE` المعتمدة.

يختلف هذا التعريف عن التنفيذ الحالي: يحتوي `profiles` حاليًا على `display_name` ودور عام من `member/admin`، بينما تحفظ عضوية المؤسسة في `organization_members` ويحفظ الفرع ونوع المستخدم في `branch_users`. الانتقال إلى النموذج الجديد يحتاج ترحيل بيانات وقرارًا صريحًا حول ما إذا كانت العضوية ستصبح مؤسسة واحدة لكل حساب، وكيف ستندمج أدوار `branch_users` مع `profiles.role`، قبل حذف أي جدول أو علاقة حالية.

### `user_preferences`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `user_id` | UUID | مفتاح أساسي وخارجي إلى `profiles.id` |
| `locale` | VARCHAR(10) | افتراضي `ar` |
| `timezone` | VARCHAR(64) | اسم IANA مثل `Asia/Riyadh` |
| `week_starts_on` | SMALLINT | من 0 إلى 6 |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |

### `organizations`

التعريف الأساسي المعتمد للمؤسسة في PostgreSQL / Supabase:

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY`، مولد في PostgreSQL |
| `name` | TEXT | اسم المؤسسة، غير فارغ |
| `status` | TEXT | حالة المؤسسة؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

يحتوي التنفيذ الحالي أيضًا على `created_by UUID` لربط المنشئ بـ`profiles.id`، و`deleted_at TIMESTAMPTZ` للحذف الناعم. يبقيان حقلي دعم حتى يُحسم هل يحل `status` محل `deleted_at` أم يعملان معًا. كما يستخدم الترحيل الحالي `VARCHAR(160)` للاسم؛ وهو متوافق وظيفيًا مع `TEXT` مع حد طول، لكن يلزم ترحيل لاحق إذا أريد التطابق الحرفي مع النوع المعتمد أعلاه.

### `organization_members`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `organization_id` | UUID | المؤسسة |
| `user_id` | UUID | المدير العضو |
| `role` | `organization_role` | `owner` أو `admin` |
| `created_at` | TIMESTAMPTZ | وقت العضوية |

المفتاح مركب من `(organization_id, user_id)`. يضيف Trigger منشئ المؤسسة بوصفه `owner`، ولا يدير تطبيق العميل العضويات مباشرة في المرحلة الثالثة.

### `branches`

التعريف الأساسي المعتمد للفرع:

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلى `organizations.id` |
| `name` | TEXT | اسم الفرع، غير فارغ |
| `code` | TEXT | رمز الفرع داخل المؤسسة، غير فارغ |
| `governorate` | TEXT | المحافظة |
| `city` | TEXT | المدينة |
| `status` | TEXT | حالة الفرع؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

القيد الإلزامي:

```sql
unique (organization_id, code)
```

يعني ذلك أن الرمز يمكن أن يتكرر بين مؤسستين، ولا يمكن أن يتكرر داخل المؤسسة نفسها. يجب تحديد ما إذا كانت مقارنة `code` حساسة لحالة الأحرف قبل إنشاء الترحيل؛ إذا لم تكن حساسة يستخدم فهرس فريد على `(organization_id, lower(code))` بدل القيد المباشر.

يحتوي التنفيذ الحالي على `name VARCHAR(160)`, و`city VARCHAR(120)`, و`deleted_at TIMESTAMPTZ`، ويضمن تفرد الاسم النشط داخل المؤسسة. لا يحتوي بعد على `code`, أو `governorate`, أو `status`؛ لذلك يحتاج اعتماد هذا التعريف في قاعدة البيانات إلى ترحيل يملأ رموزًا فريدة للفروع القائمة قبل جعل `code` إلزاميًا.

### `classes`

التعريف الأساسي المعتمد للصف:

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلى `organizations.id` |
| `branch_id` | UUID | مفتاح خارجي إلى `branches.id` |
| `name` | TEXT | اسم الصف، غير فارغ |
| `code` | TEXT | رمز الصف |
| `academic_year` | TEXT | العام الدراسي المرتبط بالصف |
| `status` | TEXT | حالة الصف؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

لأن `organization_id` يمكن اشتقاقه من `branch_id`، يجب أن تتحقق قاعدة البيانات عند الإدخال والتعديل من أن الفرع يتبع المؤسسة نفسها. يمكن تنفيذ ذلك بمفتاح خارجي مركب إذا كان لدى `branches` قيد مناسب على `(organization_id, id)`، أو بواسطة Trigger مركزي. لا يعتمد التطبيق على التحقق في Flutter وحده.

لم يعتمد حتى الآن قيد تفرد لـ`code`. يلزم تحديد هل يكون الرمز فريدًا داخل الفرع، أو المؤسسة، أو العام الدراسي قبل إنشاء الترحيل.

يحتوي التنفيذ الحالي على `branch_id`, و`name VARCHAR(160)`, وحقول الوقت و`deleted_at`، ويضمن تفرد الاسم النشط داخل الفرع. لا يحتوي بعد على `organization_id`, أو `code`, أو `academic_year`, أو `status`؛ ويتطلب اعتماد التعريف الجديد ترحيلًا يشتق المؤسسة من الفرع ويحدد قيم الحقول الجديدة للصفوف القائمة.

### `children`

جدول قديم للمرحلة الثالثة. يرحّل الترحيل الثالث صفوفه إلى `branch_users`، ولا يكتب التطبيق سجلات جديدة فيه.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `class_id` | UUID | الصف الحالي |
| `full_name` | VARCHAR(200) | اسم الطفل، بيانات شخصية |
| `birth_date` | DATE | تاريخ الميلاد الاختياري |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |
| `deleted_at` | TIMESTAMPTZ | حذف ناعم اختياري |

### `branch_users`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `branch_id` | UUID | الفرع الإلزامي |
| `class_id` | UUID | إلزامي للطفل والمعلم، وفارغ للإداري ومدير الفرع |
| `profile_id` | UUID | ربط اختياري محمي مع `profiles` ولا يعدله عميل Flutter |
| `user_type` | `managed_user_type` | `child`, `teacher`, `administrator`, `branch_manager` |
| `full_name` | VARCHAR(200) | الاسم الكامل، بيانات شخصية |
| `birth_date` | DATE | تاريخ الميلاد الاختياري |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |
| `deleted_at` | TIMESTAMPTZ | حذف ناعم اختياري |

يتحقق Trigger من أن الصف نشط ويتبع الفرع نفسه. يمنع قيد `CHECK` حفظ طفل أو معلم بلا صف، أو إسناد إداري أو مدير فرع إلى صف.

### `students`

يمثل الطالب كسجل إداري مستقل، ويمكن ربطه بحساب Supabase Auth عند الحاجة من خلال `profile_id` الاختياري.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `profile_id` | UUID | مفتاح خارجي اختياري إلى `profiles.id` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `branch_id` | UUID | مفتاح خارجي إلزامي إلى `branches.id` |
| `class_id` | UUID | مفتاح خارجي إلزامي إلى `classes.id` |
| `student_code` | TEXT | رمز الطالب الإداري |
| `first_name` | TEXT | الاسم الأول |
| `father_name` | TEXT | اسم الأب |
| `last_name` | TEXT | اسم العائلة |
| `gender` | TEXT | الجنس؛ قائمة القيم المسموحة لم تعتمد بعد |
| `login_pin_hash` | TEXT | ناتج اشتقاق PIN بخوارزمية كلمات مرور معتمدة؛ لا يخزن PIN الخام |
| `qr_identifier` | TEXT | معرف عشوائي للعثور على السجل، وليس سر مصادقة مستقلًا |
| `status` | TEXT | حالة الطالب؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

قواعد الاتساق والأمان:

- يجب أن يتبع `class_id` الفرع المحدد، وأن يتبع `branch_id` المؤسسة المحددة. يفرض ذلك داخل PostgreSQL بمفاتيح مركبة أو Trigger، وليس في Flutter فقط.
- يبقى `profile_id` اختياريًا، لكنه يجب أن يكون فريدًا بين سجلات الطلاب النشطة عند وجوده، وأن يتبع الملف المؤسسة والفرع نفسيهما.
- لا تمنح أدوار العميل صلاحية قراءة `login_pin_hash` أو كتابته مباشرة. تنفذ وظيفة خادمية موثوقة تعيين PIN والتحقق منه.
- لا يستخدم Hash سريع مثل MD5 أو SHA منفردًا لحماية PIN قصير. تعتمد الخوارزمية ومعاملاتها وسياسة تدويرها في قرار أمني مستقل.
- يطبق التحقق من PIN تحديدًا لمعدل المحاولات وقفلًا مؤقتًا وتسجيلًا أمنيًا قليل البيانات، من دون تسجيل PIN أو الهاش.
- يكون `qr_identifier` عشوائيًا وغير قابل للتخمين، ويمكن تدويره عند التسرب. مسحه يحدد الطالب فقط ثم يتطلب عامل الدخول المعتمد.
- لم تعتمد بعد قيود تفرد `student_code` و`qr_identifier`. قبل الترحيل يجب تحديد نطاق رمز الطالب، بينما ينبغي عمليًا أن يكون معرف QR فريدًا عالميًا.

يحل هذا الكيان المستهدف محل تمثيل الطفل الحالي داخل `branch_users` وجدول `children` القديم. يلزم ترحيل يحافظ على المعرفات والعلاقات والتكليفات وسجل التقدم، ولا يحذف السجلات القديمة قبل التحقق من تكافؤ البيانات واختبار RLS بحسابين منفصلين.

### `teacher_classes`

جدول ربط يسمح للمعلم بتدريس أكثر من صف، ويسمح للصف بالارتباط بأكثر من معلم.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `teacher_id` | UUID | مفتاح خارجي إلى كيان المعلم |
| `class_id` | UUID | مفتاح خارجي إلى `classes.id` |

المفتاح الأساسي المركب:

```sql
primary key (teacher_id, class_id)
```

يمنع المفتاح المركب تكرار ربط المعلم بالصف نفسه. يجب أن تتحقق قاعدة البيانات من أن المعلم والصف يتبعان المؤسسة نفسها، ومن نطاق الفروع المسموح للمعلم بالعمل فيها. عند حذف أو تعطيل معلم أو صف، تحفظ العلاقات التاريخية وفق سياسة الاحتفاظ المعتمدة ولا يعتمد على حذف غير مقصود من العميل.

لم يعتمد بعد جدول `teachers` مستقل؛ لذلك يبقى الهدف النهائي للمفتاح `teacher_id` قرارًا مفتوحًا بين `profiles.id` وجدول `teachers.id`. لا ينشأ الترحيل قبل حسمه، لأن نوع العلاقة وسياسات RLS ستختلف. يحل هذا الربط محل افتراض التنفيذ الحالي بأن المعلم يحمل `class_id` واحدًا داخل `branch_users`، ويتطلب ترحيل العلاقات القائمة إلى صف واحد على الأقل لكل معلم حالي.

### `managed_wirds`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `branch_id` | UUID | الفرع المالك للورد |
| `assigned_user_id` | UUID | المستخدم المكلّف، وفارغ في المسودة |
| `title` | VARCHAR(160) | عنوان يكتبه المدير |
| `details` | TEXT | تفاصيل خاصة اختيارية |
| `target_count` | INTEGER | الهدف العددي من 1 إلى 100000؛ مطلوب قبل التكليف |
| `status` | `managed_wird_status` | `draft`, `assigned`, `completed` |
| `created_by` | UUID | المدير المنشئ |
| `assigned_at` | TIMESTAMPTZ | وقت التكليف أو تغييره |
| `completed_at` | TIMESTAMPTZ | وقت الإنهاء |
| `created_at` / `updated_at` | TIMESTAMPTZ | أوقات السجل |
| `deleted_at` | TIMESTAMPTZ | حذف ناعم مستقبلي |

يتحقق Trigger من أن المكلّف نشط في الفرع نفسه، يضبط أوقات الانتقال، ويمنع تغيير الحقول التشغيلية بعد `completed`.

أضيفت الحقول `wird_program_id`, `dhikr_definition_id`, ولقطتا عنوان الذكر ونصه، إضافة إلى `available_from`, `available_until`, و`availability_timezone`. تبقى هذه الحقول فارغة للتكليفات الفردية القديمة.

### `wirds`

يمثل تعريف الورد الإداري القابل للتكليف، ولا يمثل تقدم مستخدم بعينه. تبقى التكليفات والعدادات الفردية في جداول منفصلة.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `dhikr_id` | UUID | مفتاح خارجي إلزامي إلى `dhikr_definitions.id` |
| `title` | TEXT | اسم الورد، وليس اسم الذكر |
| `description` | TEXT | وصف الورد الاختياري |
| `target_count` | INTEGER | الهدف العددي؛ أكبر من صفر وضمن الحد المعتمد |
| `start_at` | TIMESTAMPTZ | بداية إتاحة الورد |
| `end_at` | TIMESTAMPTZ | نهاية إتاحة الورد |
| `status` | TEXT | حالة الورد؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_by` | UUID | مفتاح خارجي إلى `profiles.id` |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

قواعد الاتساق:

- يجب أن ينتمي `dhikr_id` إلى `organization_id` نفسها وأن يكون الذكر في حالة تسمح بالتكليف.
- يجب أن يكون `end_at > start_at`، وتجرى المقارنة بتوقيت مطلق. تعرض الواجهة الوقت وفق المنطقة الزمنية المناسبة للمستخدم أو الفرع.
- يجب أن ينتمي `created_by` إلى المؤسسة وأن يملك صلاحية إنشاء الأوراد.
- لا يؤدي تعديل تعريف الذكر لاحقًا إلى تغيير النص التاريخي للتكليفات القائمة؛ تحفظ التكليفات لقطة العنوان والنص والهدف عند إنشائها.

تخزن جداول نطاق التكليف مرشحات الجمهور، وتتحقق RPC الإنشاء من المؤسسة والذكر والنطاق ثم تنشئ التكليفات الفردية ذريًا. يمنع Trigger على عمليات التقدم التسجيل خارج المدة مع السماح بإعادة العملية idempotent نفسها.

ينفذ هذا الكيان حاليًا باسم `wird_programs`، وبحقول `name`, و`dhikr_definition_id`, و`starts_on/ends_on` من نوع `DATE`، مع `timezone_name`, و`audience_name`, ولقطات الذكر. يستخدم `scope_type` نطاقات `organization`, `branch`, `multiple_branches`, `class`, `role`, و`user`، ويحفظ `wird_program_users` نطاق المستخدم. يتطلب اعتماد الاسم والحقول الجديدة ترحيلًا يحول الأيام الحالية إلى `TIMESTAMPTZ` وفق المنطقة الزمنية المحفوظة من دون تغيير مدة الأوراد القائمة، ويعيد تسمية المفاتيح والعلاقات بأمان.

### `wird_assignments`

يمثل كل صف نطاقًا إداريًا لتكليف ورد. لا يمثل تقدم المستخدم الناتج عن توسيع النطاق إلى مستفيدين أفراد.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `wird_id` | UUID | مفتاح خارجي إلزامي إلى `wirds.id` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `scope_type` | TEXT | نوع النطاق؛ قائمة القيم النهائية لم تعتمد بعد |
| `branch_id` | UUID | مفتاح خارجي اختياري إلى `branches.id` |
| `class_id` | UUID | مفتاح خارجي اختياري إلى `classes.id` |
| `role` | TEXT | دور مستهدف اختياري |
| `user_id` | UUID | مستخدم مستهدف اختياري؛ الكيان المرجعي لم يعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت إنشاء النطاق، والافتراضي `now()` |

يجب أن يفرض قيد `CHECK` شكل الصف بحسب `scope_type`:

- نطاق المؤسسة: تكون الحقول الأربعة الاختيارية فارغة.
- نطاق الفرع: يملأ `branch_id` وحده.
- نطاق الصف: يملأ `class_id` وحده.
- نطاق الدور: يملأ `role` وحده.
- نطاق المستخدم: يملأ `user_id` وحده.

عند دعم عدة فروع، يخزن كل فرع إلزاميًا في صف `wird_assignments` مستقل، ولا يسمح بحقل مثل `branch_ids` أو بقائمة نصية أو JSON أو مصفوفة معرفات داخل الصف. لا يعتمد `multiple_branches` كقيمة منفصلة؛ تمثل عدة الفروع بمجموعة صفوف من نوع `branch`.

قواعد الاتساق الإضافية:

- يجب أن تتطابق `organization_id` مع مؤسسة `wird_id`.
- يجب أن يتبع الفرع أو الصف أو المستخدم المؤسسة نفسها، وأن يتبع الصف الفرع الصحيح.
- توسيع النطاق إلى تكليفات فردية يتم ذريًا ويمنع إنشاء تكليف مكرر للمستخدم والورد نفسيهما.
- تغيير النطاق مستقبلًا لا يعيد كتابة سجلات الإنجاز التاريخية؛ توثق سياسة الإضافة والإلغاء قبل السماح بالتعديل.

يخزن التنفيذ الحالي النطاق داخل `wird_programs.scope_type` وجداول `wird_program_branches`, و`wird_program_classes`, و`wird_program_roles`, و`wird_program_users`. يتطلب الانتقال تجميع هذه العلاقات في `wird_assignments` مع الحفاظ على التكليفات الفردية الموجودة. قبل الترحيل يجب حسم مرجع `user_id`: هل هو `profiles.id` للحسابات، أم `students.id` للطلاب، أم معرف من كيان مستخدم موحد.

### `campaigns`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `dhikr_id` | UUID | مفتاح خارجي إلزامي إلى `dhikr_definitions.id` |
| `title` | TEXT | عنوان الحملة، غير فارغ |
| `description` | TEXT | وصف الحملة الاختياري |
| `target_count` | BIGINT | الهدف الجماعي، وأكبر من صفر |
| `start_at` | TIMESTAMPTZ | بداية الحملة |
| `end_at` | TIMESTAMPTZ | نهاية الحملة |
| `status` | TEXT | حالة الحملة؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_by` | UUID | مفتاح خارجي إلى `profiles.id` |

قواعد الاتساق:

- يجب أن ينتمي `dhikr_id` إلى `organization_id` نفسها وأن يكون معتمدًا عند إنشاء الحملة.
- يجب أن يكون `end_at > start_at`، ولا تقبل الزيادات الجديدة خارج المدة.
- يجب أن يكون `target_count > 0`، ولا يتجاوز التقدم المقبول الهدف.
- ينتمي `created_by` إلى المؤسسة ويملك صلاحية إنشاء الحملات.
- تحفظ مساهمة كل حدث أو مستخدم بصورة مستقلة وقابلة لإعادة المحاولة؛ لا تعتمد التقارير على رقم إجمالي يكتبه Flutter.

يحتوي التنفيذ الحالي أيضًا على `current_count BIGINT`, و`completed_at TIMESTAMPTZ`, ولقطتي عنوان الذكر ونصه. تبقى `current_count` قيمة تشغيلية مشتقة تحدثها RPC ذرية من أحداث الحملة، ولا تمنح للعميل صلاحية كتابتها. تساعد لقطة الذكر على حفظ التاريخ إذا تغير التعريف لاحقًا، ولذلك تبقى موصى بها كحقل دعم حتى إن لم تكن ضمن التعريف الأساسي.

يستخدم التنفيذ الحالي الأسماء `name` و`dhikr_definition_id` ولا يحتوي على مدة للحملة. يحتاج اعتماد النموذج الجديد إلى ترحيل يعيد التسمية ويحدد `start_at` و`end_at` للحملات القائمة دون إيقافها أو إنهائها تلقائيًا، مع الحفاظ على التقدم وعمليات idempotency السابقة.

### `campaign_mutations` — منفذ

يحفظ كل زيادة بمعرف `operation_id` فريد، مع `campaign_id`, `branch_user_id`, ولقطتي `branch_id` و`user_type`. تنفذ `apply_campaign_increment` الزيادة تحت قفل صف الحملة وتقبل إعادة العملية نفسها دون تكرار، بينما تجمع `get_campaign_dashboard` الإجمالي ومساهمات الفروع والفئات بعد التحقق من صلاحية مدير المؤسسة.

### `campaign_contributions`

ملخص مساهمة المستخدم في حملة، ويستخدم لتجميع مساهمات المستخدمين والفروع بكفاءة.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `campaign_id` | UUID | مفتاح خارجي إلزامي إلى `campaigns.id` |
| `user_id` | UUID | المستخدم المساهم؛ مرجعه يتبع قرار هوية المستخدم الموحد |
| `branch_id` | UUID | مفتاح خارجي إلزامي إلى `branches.id`، كلقطة فرع المساهم |
| `count` | BIGINT | مجموع المساهمة المقبولة، وصفر أو أكبر |
| `updated_at` | TIMESTAMPTZ | وقت آخر تحديث للمساهمة، ويضبطه الخادم |

قواعد السلامة:

- يجب أن تتبع الحملة والمستخدم والفرع المؤسسة نفسها.
- يمثل `branch_id` فرع المستخدم وقت المساهمة. لا تعيد تغييرات فرع المستخدم اللاحقة كتابة التقارير التاريخية.
- يفرض `check (count >= 0)`، ولا يتجاوز مجموع مساهمات الحملة هدفها وفق سياسة الحملة.
- لا يكتب Flutter قيمة `count` مباشرة. تستقبل RPC حدثًا ذا معرف فريد، ثم تحدث سجل المساهمة وإجمالي الحملة ذريًا.
- تبقى أحداث المساهمة الفردية هي سجل idempotency والتدقيق؛ `campaign_contributions` ملخص قابل لإعادة البناء منها.
- تقيد RLS القراءة التفصيلية بالإدارة المخولة، بينما يمكن للمستخدم قراءة مساهمته فقط إذا اعتمد المنتج ذلك.

إذا كان المطلوب صفًا واحدًا لكل مستخدم في الحملة، يعتمد القيد:

```sql
unique (campaign_id, user_id)
```

لم يحسم هذا القيد صراحة بعد. إذا أريد أكثر من صف للمستخدم نفسه، فيجب إضافة بعد واضح مثل اليوم أو الفرع التاريخي، بدل إنشاء صفوف متكررة بلا مفتاح عمل.

لا يوجد هذا الجدول حاليًا؛ تجمع لوحة الحملة مساهمات `campaign_mutations` مباشرة. يتطلب إنشاؤه ترحيلًا تجميعيًا من الأحداث القائمة داخل معاملة، ثم مقارنة إجمالي الملخص بإجمالي الحملة قبل تفعيل القراءة منه.

### `audit_logs`

سجل تدقيق إداري append-only يوثق العمليات الحساسة ومن نفذها والكيان المتأثر داخل المؤسسة.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `actor_id` | UUID | مفتاح خارجي إلى `profiles.id` للفاعل الذي نفذ العملية |
| `action` | TEXT | رمز العملية من قائمة معتمدة، مثل الإنشاء أو التعديل أو التعطيل |
| `entity_type` | TEXT | نوع الكيان من قائمة معتمدة |
| `entity_id` | UUID | معرف الكيان المتأثر |
| `metadata` | JSONB | بيانات سياقية محدودة وغير حساسة تساعد في المراجعة |
| `created_at` | TIMESTAMPTZ | وقت تسجيل العملية على الخادم، والافتراضي `now()` |

قواعد الأمان والتشغيل:

- الجدول غير قابل للتعديل: لا توجد عمليات `UPDATE` أو `DELETE` لأدوار التطبيق، ولا يكتب Flutter صفوفه مباشرة.
- تكتب وظيفة خادمية السجل داخل المعاملة نفسها التي تنفذ العملية الإدارية، حتى لا تنجح العملية من دون أثر تدقيق أو يظهر أثر لعملية تراجعت.
- يجب أن ينتمي `actor_id` إلى `organization_id` وأن يملك الصلاحية المطلوبة وقت التنفيذ. إذا احتاج النظام لاحقًا إلى فاعل آلي، يعتمد تمثيل صريح له بدل اختلاق معرف مستخدم.
- تكون `action` و`entity_type` رموزًا ثابتة قابلة للفهرسة، ولا تخزن أو تنفذ SQL ديناميكيًا.
- لا يحتوي `metadata` كلمات مرور أو PIN أو `login_pin_hash` أو رموز جلسة أو مفاتيح API أو معرفات idempotency غير اللازمة أو نصوصًا شخصية كاملة. تحفظ التغييرات الحساسة على شكل أسماء حقول أو قيم منزوعة الحساسية عند الحاجة.
- لا يعد `metadata` بديلًا للأعمدة والعلاقات الطبيعية، ولا تخزن فيه مفاتيح أجنبية تحتاج إلى استعلامات تشغيلية.
- تقيد RLS القراءة بالمدير المخول في المؤسسة. ويمكن حصر السجل الكامل بالمدير المركزي إذا تطلبت سياسة الخصوصية ذلك.
- تحدد سياسة احتفاظ وأرشفة قبل الإنتاج؛ لا يحذف السجل تعسفيًا من لوحة الإدارة.

الفهارس الموصى بها:

```sql
create index audit_logs_org_created_idx
  on audit_logs (organization_id, created_at desc);

create index audit_logs_entity_idx
  on audit_logs (organization_id, entity_type, entity_id, created_at desc);

create index audit_logs_actor_idx
  on audit_logs (organization_id, actor_id, created_at desc);
```

لا يوجد سجل تدقيق عام في التنفيذ الحالي. يحتاج اعتماده إلى ترحيل جديد وربط العمليات الإدارية الأساسية بوظيفة تسجيل موحدة، مع اختبارات تثبت عزل المؤسسات وعدم قدرة المدير أو العميل على تعديل السجلات السابقة.

### `managed_wird_progress`

يحفظ العدد البعيد لكل ورد مكلّف ويرتبط بالمستخدم المكلّف. لا يكتب العميل إليه مباشرة.

### `user_wird_progress`

يمثل تقدم مستخدم في ورد خلال يوم محلي محدد، ويحفظ لقطة الهدف لذلك اليوم حتى لا تغير تعديلات الورد اللاحقة السجل التاريخي.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `branch_id` | UUID | مفتاح خارجي إلزامي إلى `branches.id` |
| `user_id` | UUID | المستخدم صاحب التقدم؛ الكيان المرجعي النهائي لم يعتمد بعد |
| `wird_id` | UUID | مفتاح خارجي إلزامي إلى `wirds.id` |
| `progress_date` | DATE | اليوم المحلي الذي ينتمي إليه التقدم |
| `target_count` | INTEGER | لقطة الهدف لذلك اليوم، وأكبر من صفر |
| `completed_count` | INTEGER | العدد المنجز، من صفر إلى `target_count` |
| `is_completed` | BOOLEAN | `true` فقط عندما يصل العدد المنجز إلى الهدف |
| `first_activity_at` | TIMESTAMPTZ | وقت أول نشاط مقبول |
| `last_activity_at` | TIMESTAMPTZ | وقت آخر نشاط مقبول |
| `last_synced_at` | TIMESTAMPTZ | وقت آخر مزامنة ناجحة مع الخادم |
| `created_at` | TIMESTAMPTZ | وقت إنشاء السجل، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

القيد الفريد الإلزامي:

```sql
unique (user_id, wird_id, progress_date)
```

قيود السلامة:

```sql
check (target_count > 0)
check (completed_count between 0 and target_count)
check (is_completed = (completed_count >= target_count))
check (
  first_activity_at is null
  or last_activity_at is null
  or last_activity_at >= first_activity_at
)
```

قواعد التشغيل:

- يجب أن تتطابق المؤسسة والفرع مع الورد والمستخدم، وتفرض قاعدة البيانات هذا الاتساق.
- يحسب `progress_date` وفق المنطقة الزمنية المحددة للورد أو الفرع ويحفظ اسم المنطقة في مصدر موثوق؛ لا يستخدم تاريخ UTC مباشرة بوصفه يوم المستخدم.
- تكون أوقات النشاط فارغة قبل أول ضغطة. تضبط أول ضغطة `first_activity_at` و`last_activity_at`، ثم يتغير وقت النشاط الأخير فقط.
- لا يعد `last_synced_at` دليلًا على اكتمال الورد؛ هو وقت تأكيد آخر مزامنة ناجحة فقط.
- تطبق الزيادات من خلال RPC ذرية ومعرف عملية فريد، ولا يمنح Flutter كتابة العدد مباشرة.
- يبقى مرجع `user_id` بحاجة إلى حسم بين `profiles.id`, و`students.id`، أو كيان مستخدم موحد، ويجب أن يتطابق مع القرار المستخدم في `wird_assignments.user_id`.

يختلف هذا النموذج عن `managed_wird_progress` الحالي الذي يحفظ صفًا واحدًا لكل تكليف ولا يحتوي على `progress_date` أو لقطة المؤسسة والفرع. يحتاج الانتقال إلى ترحيل يحول كل تقدم قائم إلى يومه المحلي الصحيح، ويحافظ على العدد والهدف وأوقات الإكمال، ثم يعيد ربط طابور العمليات من دون تكرار الزيادات.

### `managed_wird_mutations`

يحفظ `operation_id` فريدًا لكل ضغطة مقبولة لمنع تطبيق الزيادة مرتين عند إعادة الطلب.

### `sync_events`

سجل أحداث append-only يستقبل زيادات التقدم القادمة من الأجهزة، ويمنع تطبيق الحدث نفسه أكثر من مرة عند انقطاع الاستجابة أو إعادة المحاولة.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `user_id` | UUID | المستخدم صاحب الحدث؛ مرجعه يتبع القرار الموحد لهوية المستخدم |
| `progress_id` | UUID | مفتاح خارجي إلى `user_wird_progress.id` |
| `device_id` | TEXT | معرف تقني مستعار للجهاز أو تثبيت التطبيق |
| `delta` | INTEGER | مقدار الزيادة التي يحملها الحدث |
| `client_timestamp` | TIMESTAMPTZ | وقت إنشاء الحدث بحسب الجهاز، وهو غير موثوق |
| `server_timestamp` | TIMESTAMPTZ | وقت قبول الخادم للحدث، والافتراضي `now()` |
| `event_uuid` | UUID | معرف idempotency فريد ينشئه العميل مرة واحدة |
| `created_at` | TIMESTAMPTZ | وقت إنشاء السجل في قاعدة البيانات، والافتراضي `now()` |

القيد الإلزامي:

```sql
unique (event_uuid)
```

مسار المعالجة الذري:

```text
استقبال event_uuid
→ محاولة إنشاء sync_events
→ إذا كان موجودًا: إرجاع النتيجة الحالية دون تطبيق delta
→ إذا كان جديدًا: قفل صف progress
→ التحقق من الملكية والورد والمدة والحد
→ تطبيق delta مرة واحدة
→ تحديث last_activity_at وlast_synced_at
→ Commit
```

قواعد السلامة والخصوصية:

- لا تمنح Flutter صلاحية الإدخال المباشر في الجدول أو تعديل `completed_count`. تستقبل RPC الحدث وتنفذ الإنشاء والزيادة داخل معاملة واحدة.
- يجب أن يتطابق `user_id` مع مالك `progress_id` ومع `auth.uid()` أو آلية دخول الطالب المعتمدة.
- في مسار عداد الذكر الحالي يكون `delta > 0`. إذا لزم التصحيح السالب مستقبلًا فينفذ كعملية إدارية منفصلة ومدققة، ولا يقبل من العميل العادي.
- يقيد مجموع التقدم بالهدف ولا يسمح بتجاوزه. إعادة `event_uuid` نفسه لا تغير العدد حتى لو أرسل العميل بيانات مختلفة معه؛ يمكن وسم اختلاف الحمولة كمؤشر `sync_anomaly` للمراجعة فقط.
- لا يعتمد الخادم على `client_timestamp` لتقرير ترتيب نهائي أو صلاحية الحدث وحده، لأنه قابل للخطأ أو التلاعب. يكون `server_timestamp` مرجع الاستلام.
- `device_id` معرف مستعار قابل للتدوير، وليس رقم جهاز دائمًا أو سر دخول، ولا يستخدم لتتبع الموقع أو لصنع تصنيف عقابي.
- لا يحذف الحدث بعد المزامنة مباشرة؛ تحدد سياسة احتفاظ زمنية قبل التنظيف مع إبقاء ضمان idempotency مدة كافية لإعادات المحاولة المتأخرة.

ينفذ المفهوم حاليًا عبر `managed_wird_mutations.operation_id` وطابور Drift المحلي `dhikr_mutations`. يتطلب الانتقال إلى `sync_events` ترحيل معرفات العمليات الحالية إلى `event_uuid` وربطها بسجلات `user_wird_progress` الجديدة من دون إعادة تطبيق أي زيادة سبق قبولها.

### `child_reward_summaries`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `branch_user_id` | UUID | الطفل صاحب المكافأة |
| `points` | INTEGER | مجموع لقطات الجوائز المستحقة |
| `completed_wirds` | INTEGER | عدد الأوراد المكتملة |
| `badge` | VARCHAR(40) | `first_wird` بعد أول إتمام |
| `current_streak` | INTEGER | طول الاستمرارية المحسوبة عند آخر يوم التزام |
| `best_streak` | INTEGER | أفضل استمرارية مسجلة |
| `last_compliance_day` | DATE | آخر يوم اكتملت فيه جميع الأوراد المستحقة |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |

### `point_rules`

يخزن قواعد النقاط القابلة للتعديل: رمز القاعدة، نوع الحدث، قيمة النقاط، حد أيام الاستمرارية الاختياري، وحالة التفعيل. القيم الأولية هي +10 لإتمام ورد، +10 لإتمام أوراد اليوم، ثم +10 و+30 و+100 عند 3 و7 و30 يومًا.

### `point_awards`

دفتر الجوائز غير القابل للاشتقاق من عدد الضغطات. يحفظ الطفل والقاعدة والورد أو يوم الالتزام و`points_snapshot` و`award_key` فريدًا. يضمن القيد الفريد عدم تكرار الجائزة عند إعادة المزامنة، وتبقى الجائزة التاريخية كما كانت عند تعديل القاعدة مستقبلًا.

### `user_compliance_days`

يحفظ يوم الالتزام حسب منطقة الورد الزمنية، وعدد الأوراد المكتملة، وطول الاستمرارية. لا ينشأ اليوم إلا بعد اكتمال جميع أوراد الطفل المستحقة لذلك اليوم.

تنفذ `apply_assigned_wird_increment` التحقق من الحساب ومنع تكرار العملية وزيادة التقدم. وعند انتقال الورد لأول مرة إلى الإكمال تستدعي `award_child_compliance_points` داخل المعاملة نفسها؛ لا تمنح الضغطات الجزئية نقاطًا.

### `badges`

تعريفات الأوسمة الخاصة بكل مؤسسة والقواعد الأساسية لمنحها.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `name` | TEXT | اسم الوسام، غير فارغ |
| `description` | TEXT | وصف سبب استحقاق الوسام |
| `icon_key` | TEXT | مفتاح من مكتبة أيقونات معتمدة في التطبيق، وليس رابطًا حرًا |
| `rule_type` | TEXT | نوع قاعدة المنح؛ قائمة القيم المسموحة لم تعتمد نهائيًا |
| `rule_value` | INTEGER | قيمة القاعدة العددية وتفسر بحسب `rule_type` |
| `status` | TEXT | حالة الوسام؛ قائمة القيم المسموحة لم تعتمد بعد |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |

قواعد السلامة:

- يجب ألا ينفذ `rule_type` بوصفه SQL أو تعبيرًا ديناميكيًا. يقبل محرك المنح قيمًا معروفة فقط ويربط كل قيمة بمنطق خادمي مختبر.
- يتحقق الخادم من `rule_value` بحسب نوع القاعدة؛ مثلًا تكون عتبة الاستمرارية موجبة. يحتاج الوسام اليدوي إلى قرار بشأن قيمة `rule_value` إذا بقي الحقل إلزاميًا.
- يقيد RLS التعريفات بالمؤسسة، ولا يستطيع مدير مؤسسة إنشاء وسام أو منحه داخل مؤسسة أخرى.
- يكون `icon_key` واحدًا من مفاتيح أصول التطبيق المعتمدة، لتجنب تحميل محتوى خارجي غير موثوق داخل تجربة الطفل.
- لا تستخدم الأوسمة لتقديم ترتيب سلبي للطفل، وتبقى قواعد المنح قابلة للتعديل من قاعدة البيانات وفق سياسة المنتج.

ينفذ هذا الجدول حاليًا باسم `badge_definitions` كمكتبة مشتركة، ويحتوي على `code`, و`emoji`, و`rule_type`, و`streak_days`, و`is_active`, و`sort_order`. يحتاج الانتقال إلى `badges` ترحيلًا يحدد المؤسسة المالكة لكل تعريف ويحافظ على مراجع `badge_awards`. يجب تحديد ما إذا كانت الأوسمة الأساسية ستنسخ لكل مؤسسة أم ستبقى في مكتبة عامة منفصلة قبل جعل `organization_id` إلزاميًا.

### `user_badges`

يحفظ الأوسمة التي حصل عليها المستخدم وتوقيت منحها.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `user_id` | UUID | المستخدم الحاصل على الوسام؛ مرجعه يتبع قرار هوية المستخدم الموحد |
| `badge_id` | UUID | مفتاح خارجي إلزامي إلى `badges.id` |
| `awarded_at` | TIMESTAMPTZ | وقت المنح، والافتراضي `now()` عند المنح الخادمي |

يجب أن يتحقق الخادم من أن المستخدم والوسام يتبعان المؤسسة نفسها، وأن قاعدة الوسام تحققت أو أن المانح يملك صلاحية المنح اليدوي. لا يمنح Flutter الوسام بإدخال مباشر، بل من خلال Trigger أو RPC خادمية قابلة لإعادة المحاولة.

لم يحدد بعد هل يمكن للمستخدم الحصول على الوسام نفسه أكثر من مرة. إذا كان المنح مرة واحدة فقط، يعتمد القيد:

```sql
unique (user_id, badge_id)
```

أما إذا كانت الأوسمة قابلة للتكرار فيجب إضافة معرف للسياق أو الدورة التي استحق فيها الوسام، بدل السماح بصفوف مكررة بلا تفسير.

ينفذ هذا المفهوم حاليًا باسم `badge_awards` ويرتبط بـ`branch_user_id` و`badge_definitions`. يتطلب الانتقال إلى `user_badges` حسم مرجع `user_id` ثم ترحيل المنح الحالية مع المحافظة على أوقات المنح ومنع التكرار غير المقصود.

### `get_branch_child_rewards`

RPC إدارية تقبل `branch_id` بعد التحقق من `can_manage_branch`، ثم تعيد لكل طفل نشط اسمه وصفه وعدد أوراده ونقاطه واستمراريته وأوسمته. تشمل الأطفال الذين لا يملكون ملخصًا بعد بقيم صفرية، وترتب النتائج بالاسم فقط. لا يعاد أي حقل للترتيب أو المركز أو المقارنة بين الأطفال.

### `wird_plans`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `user_id` | UUID | مالك الخطة |
| `title` | VARCHAR(120) | عنوان غير فارغ |
| `description` | TEXT | اختياري |
| `status` | VARCHAR(20) | `active`, `paused`, `archived` |
| `days_of_week` | SMALLINT[] | أيام التكرار، قيمها 0–6 |
| `starts_on` | DATE | أول يوم للخطة |
| `ends_on` | DATE | اختياري ويجب ألا يسبق البداية |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |
| `deleted_at` | TIMESTAMPTZ | حذف ناعم اختياري للمزامنة |

### `content_sources`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `name` | VARCHAR(160) | اسم المصدر |
| `publisher` | VARCHAR(160) | الناشر أو الجهة |
| `reference_url` | TEXT | رابط مرجعي اختياري |
| `version` | VARCHAR(60) | إصدار المصدر |
| `reviewed_at` | TIMESTAMPTZ | وقت آخر مراجعة |
| `reviewed_by` | VARCHAR(120) | معرف المراجع وفق السياسة |

### `verified_contents`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `source_id` | UUID | مصدر المحتوى |
| `content_type` | VARCHAR(30) | مثل `quran_reference` أو `dhikr` |
| `title` | VARCHAR(160) | عنوان العرض |
| `body` | TEXT | النص المراجع عند الحاجة |
| `reference_code` | VARCHAR(120) | مرجع السورة/الآية أو المصدر |
| `status` | VARCHAR(20) | `draft`, `reviewed`, `retired` |
| `checksum` | VARCHAR(128) | للتحقق من تغير النص |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |

### `dhikr_definitions`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي `PRIMARY KEY` |
| `organization_id` | UUID | مفتاح خارجي إلزامي إلى `organizations.id` |
| `title` | TEXT | عنوان التعريف، غير فارغ |
| `display_text` | TEXT | النص الذي يعرض بعد الاعتماد، غير فارغ |
| `description` | TEXT | وصف اختياري |
| `default_target` | INTEGER | هدف افتراضي من 1 إلى 100000 |
| `status` | TEXT | حالة التعريف؛ التنفيذ الحالي يستخدم `draft`, `under_review`, `approved`, `archived` |
| `created_by` | UUID | مفتاح خارجي إلى `profiles.id` |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء، والافتراضي `now()` |
| `updated_at` | TIMESTAMPTZ | آخر تحديث، ويضبطه Trigger |

حقول حوكمة المحتوى الإلزامية في المنتج، إضافة إلى التعريف الأساسي:

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `source_reference` | TEXT | مرجع مطلوب للاعتماد |
| `content_version` | INTEGER | يرتفع تلقائيًا عند تغير المحتوى أو المصدر |
| `content_checksum` | TEXT | بصمة مولدة من المحتوى والهدف |
| `reviewed_by` / `reviewed_at` | UUID / TIMESTAMPTZ | سجل المراجعة البشرية |

تقيد RLS كل قراءة وكتابة بـ`organization_id`. يقرأ المستخدم التعريفات المعتمدة داخل مؤسسته فقط، بينما يقرأ المدير المخول حالات مؤسسته ويديرها. يعيد Trigger أي تعريف معتمد إلى `under_review` عند تغير محتواه أو مصدره، ولا يسمح بإعادة اعتماده بلا مرجع ومراجع بشري.

التنفيذ الحالي يجعل المكتبة مشتركة ولا يحتوي على `organization_id`، ويستخدم `VARCHAR(160)` للعنوان ونوع Enum للحالة. يتطلب النموذج الجديد ترحيلًا يحدد مؤسسة لكل تعريف قائم قبل جعل المفتاح الخارجي إلزاميًا، ويجب تحديد سياسة التعامل مع التعريفات العامة المشتركة إن كانت ستبقى مطلوبة. لا تنسخ التعريفات أو تنسبها إلى مؤسسة تلقائيًا قبل قرار مالك المحتوى.

### `plan_items`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `plan_id` | UUID | الخطة التابعة |
| `verified_content_id` | UUID | اختياري للمحتوى المراجع |
| `item_type` | VARCHAR(20) | `reading`, `dhikr`, `custom` |
| `title` | VARCHAR(160) | عنوان العنصر |
| `target_value` | INTEGER | أكبر من صفر |
| `unit` | VARCHAR(30) | مثل `page`, `verse`, `repeat`, `item` |
| `position` | INTEGER | صفر أو أكبر |
| `is_active` | BOOLEAN | افتراضي `true` |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |
| `deleted_at` | TIMESTAMPTZ | حذف ناعم اختياري للمزامنة |

إذا كان `verified_content_id` فارغًا، فالعنوان محتوى أنشأه المستخدم ولا يجوز عرضه بوصفه نصًا موثقًا.

### `daily_days`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `user_id` | UUID | مالك اليوم |
| `local_date` | DATE | التاريخ في منطقة المستخدم |
| `timezone` | VARCHAR(64) | لقطة المنطقة الزمنية عند الإنشاء |
| `status` | VARCHAR(20) | `pending`, `in_progress`, `completed` |
| `completed_at` | TIMESTAMPTZ | اختياري |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |

قيد فريد: `UNIQUE (user_id, local_date)`.

### `daily_items`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `daily_day_id` | UUID | اليوم التابع |
| `plan_item_id` | UUID | العنصر الأصلي |
| `title_snapshot` | VARCHAR(160) | لقطة تحفظ التاريخ |
| `target_snapshot` | INTEGER | الهدف وقت إنشاء اليوم |
| `unit_snapshot` | VARCHAR(30) | الوحدة وقت إنشاء اليوم |
| `progress_value` | INTEGER | افتراضي 0 وغير سالب |
| `status` | VARCHAR(20) | `pending`, `in_progress`, `completed` |
| `completed_at` | TIMESTAMPTZ | اختياري |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |

قيد فريد: `UNIQUE (daily_day_id, plan_item_id)`. يجب أن يساوي اكتمال العنصر بلوغ الهدف وفق قاعدة المنتج المعتمدة.

### `reminders`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `user_id` | UUID | مالك التذكير |
| `channel` | VARCHAR(20) | `email` أو `web_push` |
| `local_time` | TIME | وقت المستخدم المحلي |
| `timezone` | VARCHAR(64) | اسم IANA |
| `days_of_week` | SMALLINT[] | أيام الإرسال |
| `is_enabled` | BOOLEAN | حالة التذكير |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |
| `deleted_at` | TIMESTAMPTZ | حذف ناعم اختياري للمزامنة |

### `reminder_deliveries`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | مفتاح أساسي |
| `reminder_id` | UUID | التذكير |
| `local_date` | DATE | يوم الإرسال المقصود |
| `scheduled_for` | TIMESTAMPTZ | الموعد المحسوب |
| `status` | VARCHAR(20) | `queued`, `sent`, `failed`, `skipped` |
| `attempt_count` | SMALLINT | افتراضي 0 |
| `provider_message_id` | VARCHAR(200) | اختياري |
| `last_error_code` | VARCHAR(80) | رمز منقح، لا رسالة حساسة |
| `sent_at` | TIMESTAMPTZ | اختياري |
| `created_at` | TIMESTAMPTZ | وقت الإنشاء |
| `updated_at` | TIMESTAMPTZ | آخر تحديث |

قيد فريد: `UNIQUE (reminder_id, local_date)` لمنع الإرسال المكرر.

### `client_mutations`

سجل بعيد صغير يجعل عمليات Flutter المعاد إرسالها قابلة للتكرار الآمن.

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `operation_id` | UUID | مفتاح أساسي يولده العميل |
| `user_id` | UUID | صاحب العملية، يساوي `auth.uid()` |
| `entity_type` | VARCHAR(40) | نوع الكيان المتأثر |
| `entity_id` | UUID | معرف الكيان |
| `operation_type` | VARCHAR(40) | مثل `increment_progress` |
| `result_payload` | JSONB | النتيجة اللازمة لإعادة رد مطابق بأمان |
| `created_at` | TIMESTAMPTZ | وقت قبول العملية |

تتعامل RPC مع إدخال سجل العملية وتحديث الكيان في معاملة واحدة. لا تُخزن حمولة حساسة لا تحتاجها إعادة المحاولة.

### `dhikr_sessions`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `id` | UUID | يولده العميل قبل الحفظ المحلي |
| `user_id` | UUID | مالك العداد، يساوي `auth.uid()` |
| `title` | VARCHAR(160) | عنوان خاص يكتبه المستخدم |
| `target` | INTEGER | من 1 إلى 100000 |
| `count` | INTEGER | من صفر إلى الهدف |
| `status` | `dhikr_completion_status` | `in_progress` أو `completed` |
| `client_created_at` | TIMESTAMPTZ | وقت إنشاء الجلسة على الجهاز |
| `completed_at` | TIMESTAMPTZ | يملأ عند بلوغ الهدف |
| `created_at` / `updated_at` | TIMESTAMPTZ | أوقات الخادم |

### `dhikr_mutations`

| الحقل | النوع | القيود/الغرض |
|---|---|---|
| `operation_id` | UUID | مفتاح أساسي ثابت يمنع تطبيق الزيادة مرتين |
| `user_id` | UUID | مالك العملية |
| `session_id` | UUID | جلسة العداد |
| `delta` | INTEGER | الزيادة الموجبة |
| `client_created_at` | TIMESTAMPTZ | ترتيب العملية محليًا |
| `created_at` | TIMESTAMPTZ | وقت قبول الخادم |

لا يكتب العميل الجدولين مباشرة. تنفذ `apply_dhikr_increment` التحقق من الهوية، وإدخال العملية، وزيادة العداد في معاملة واحدة. إعادة `operation_id` نفسه تعيد النتيجة الحالية دون زيادة جديدة.

محليًا يمثل `dhikr_mutations` طابور المزامنة الفعلي للعداد: يبقى `synced_at` فارغًا حتى تؤكد RPC النجاح، ويحفظ `attempt_count` محاولات الفشل. يحدث `dhikr_sessions.count` وإدخال mutation في معاملة Drift واحدة، ويستخدم ملف SQLite الدائم نفسه بعد إعادة تشغيل التطبيق أو الهاتف.

### `integrity_rules`

قواعد قابلة للتعديل للأنواع `unusually_fast_activity`, `excessive_activity`, و`sync_anomaly`. يحفظ كل نوع نافذة زمنية وعتبة عددية وحالة تفعيل. القيم الأولية المحافظة هي 20 عملية خلال 5 ثوانٍ، و5000 خلال 24 ساعة، و10 محاولات للعملية نفسها.

### `integrity_flags`

سجل إداري غير عقابي يحفظ المستخدم والفرع والمؤسسة ونوع المؤشر وحالته والعدد المرصود ولقطة العتبة والنافذة الزمنية ودليلًا رقميًا مختصرًا. لا يحتوي مفاتيح حذف أو تحديث للإنجاز. الحالات `open`, `reviewed`, و`dismissed`، وتخزن هوية المراجع ووقت المراجعة دون حذف الصف.

### `managed_wird_sync_attempts`

يحصي محاولات استقبال `operation_id` نفسه لاكتشاف `sync_anomaly`. لا يغير mutation الأصلية ولا تقدم الورد. تخفي RLS الجدول عن العميل بالكامل.

تنفذ `get_branch_integrity_flags` القراءة بعد `can_manage_branch`، وتنفذ `review_integrity_flag` تغيير حالة العلامة فقط. لا توجد سياسة قراءة للمستخدم صاحب النشاط.

## 4. الفهارس الأساسية

- `wird_plans (user_id, status)`.
- `plan_items (plan_id, is_active, position)`.
- `daily_days (user_id, local_date DESC)`.
- `daily_items (daily_day_id, status)`.
- `reminders (is_enabled, timezone)`.
- `reminder_deliveries (status, scheduled_for)` للبحث عن الرسائل المستحقة.
- `verified_contents (status, content_type)`.
- `client_mutations (user_id, created_at DESC)` مع سياسة احتفاظ محددة.
- `dhikr_sessions (user_id, updated_at DESC)`.
- `dhikr_mutations (user_id, created_at DESC)`.
- `organization_members (user_id, organization_id)`.
- `branches (organization_id, name)` للسجلات النشطة.
- `classes (branch_id, name)` للسجلات النشطة.
- `children (class_id, full_name)` للسجلات النشطة.
- `branch_users (branch_id, user_type, full_name)` للسجلات النشطة.
- `branch_users (class_id, user_type, full_name)` للمرتبطين بصف، مع تفرد `profile_id` النشط عند وجوده.
- `managed_wirds (branch_id, status, updated_at DESC)` للسجلات النشطة.
- `managed_wirds (assigned_user_id, status, updated_at DESC)` للأوراد المكلّفة.
- `managed_wird_mutations (branch_user_id, created_at DESC)` لمراجعة العمليات المطبقة.

## 5. سياسات Supabase RLS

### المبدأ الملزم

تكون PostgreSQL / Supabase هي الحد الأمني النهائي. لا تعد فلاتر Flutter أو إخفاء الأزرار أو GoRouter وسيلة حماية للبيانات. يجب أن يفشل الطلب غير المصرح به داخل قاعدة البيانات حتى لو استدعي REST أو RPC مباشرة من تطبيق معدل.

| الدور | نطاق القراءة والكتابة المسموح |
|---|---|
| الطالب `Student` | بياناته وتقدمه وأوراده وأوسمته فقط |
| المعلم `Teacher` | بياناته، والصفوف المرتبط بها في `teacher_classes` والطلاب المسموحين داخلها فقط |
| مدير الفرع `Branch Manager` | بيانات فرعه المحدد في `profiles.branch_id` فقط |
| مدير المؤسسة `Organization Admin` | جميع فروع مؤسسته المحددة في `profiles.organization_id` فقط |

يعتمد كل جدول خاص بالمؤسسة `organization_id`، وتضيف الجداول الفرعية `branch_id` أو علاقة أب يمكن تتبعها إلى المؤسسة. تفعّل RLS وتكتب سياسات مستقلة للقراءة والإنشاء والتعديل والحذف؛ سياسة `SELECT` وحدها لا تحمي الكتابة.

### دوال الصلاحية الموحدة

تعرّف دوال خادمية صغيرة ومختبرة بدل تكرار شروط مختلفة بين السياسات، مثل:

- `current_profile()` لإرجاع الملف الفعال بعد التحقق من `auth.uid()`.
- `can_access_organization(target_organization_id)` لمدير المؤسسة داخل مؤسسته فقط.
- `can_access_branch(target_organization_id, target_branch_id)` لمدير المؤسسة، أو مدير الفرع المطابق فقط.
- `can_teach_class(target_class_id)` للتحقق من وجود `(teacher_id, class_id)` داخل `teacher_classes` ومن تطابق المؤسسة.
- `is_own_user(target_user_id)` للتحقق من أن السجل يخص هوية الجلسة نفسها.

إذا استخدمت الدوال `security definer` فيجب تثبيت `search_path = ''` وتأهيل أسماء الجداول بالمخطط، وسحب التنفيذ من `public` ومنحه فقط للدور اللازم. لا تقبل الدالة `organization_id` أو `role` من العميل بوصفهما دليل صلاحية؛ تستخرج هوية المتصل من JWT وتتحقق من قاعدة البيانات.

### الطالب وتسجيل PIN

يعمل شرط «بياناته فقط» مباشرة عندما يرتبط `students.profile_id` بـ`auth.uid()`. أما الطالب الذي يدخل بـPIN وسجله لا يحمل `profile_id` فلا يمنح وصولًا مباشرًا مجهولًا إلى جداول Supabase. يجب أن تتحقق وظيفة خادمية من PIN ثم تصدر جلسة موثوقة محدودة تحمل معرف الطالب والمعلومات اللازمة لـRLS، أو تنفذ العمليات عبر واجهة خادمية لا تكشف الجداول. لا يقبل `student_id` المرسل من Flutter وحده إثباتًا للهوية.

### مثال الرفض الإلزامي

إذا كان ملف المتصل مديرًا لفرع دمشق:

```text
profiles.branch_id = damascus_branch_id
```

ثم طلب قراءة طلاب فرع حلب أو تقريره:

```text
requested branch_id = aleppo_branch_id
```

فيجب أن تعيد سياسة RLS صفر صفوف للقراءة المباشرة، وأن ترفض RPC الإدارية الطلب بخطأ صلاحية قبل تنفيذ الاستعلام. لا يكفي ألا يظهر فرع حلب في القائمة داخل Flutter.

### اختبارات RLS الإلزامية

- الطالب (أ) لا يقرأ أو يعدل تقدم الطالب (ب)، حتى داخل الصف نفسه.
- المعلم لا يقرأ صفًا غير موجود في `teacher_classes` الخاصة به.
- معلم مرتبط بصفين يقرأ الصفين فقط، ولا يكتسب صلاحية بقية الفرع.
- مدير دمشق لا يقرأ أو يعدل فرع حلب داخل المؤسسة نفسها.
- مدير مؤسسة لا يقرأ مؤسسة أخرى حتى عند تخمين UUID صحيح.
- المدير المركزي ذو `branch_id = NULL` لا يتجاوز `organization_id` الخاص به.
- تغيير `organization_id`, و`branch_id`, و`role`, و`status` من العميل مرفوض.
- RPC وواجهات REST تعطيان النتيجة الأمنية نفسها، وتختبر عمليات `SELECT`, و`INSERT`, و`UPDATE`, و`DELETE` كل على حدة.
- يستخدم الاختبار حسابين أو أكثر فعليين ولا يعتمد على `service_role`، لأن `service_role` يتجاوز RLS.

### حالة التنفيذ الحالية

ينفذ المخطط الحالي عزل الطالب لبياناته، وعزل المعلم لصف واحد، وعزل المؤسسات عبر دوال `can_manage_*`. لكنه يمنح مدير المؤسسة المخول إدارة فروع المؤسسة كلها، ولا يمثل بعد دور مدير فرع مقيدًا بـ`profiles.branch_id` وفق المخطط المستهدف. لذلك لا يعد سيناريو «مدير دمشق مرفوض من حلب» منفذًا بالكامل قبل ترحيل `profiles` والأدوار وسياسات RLS الجديدة واختبارها على Supabase فعلي.

- تفعّل RLS على `profiles`, `user_preferences`, `wird_plans`, `plan_items`, `daily_days`, `daily_items`, `reminders`, `reminder_deliveries`, و`client_mutations`.
- تسمح السياسات بالقراءة والكتابة فقط عندما تطابق الملكية `auth.uid()`، مباشرة أو عبر الجدول الأب.
- المحتوى الموثق المنشور قابل للقراءة، بينما إنشاؤه ومراجعته يحتاجان دورًا إداريًا من الخادم.
- العمليات المميزة مثل تطبيق تقدم متزامن أو حذف الحساب تنفذ عبر RPC أو Edge Function تتحقق من الهوية.
- تُختبر السياسات بحسابين على الأقل للتأكد من استحالة القراءة أو التعديل المتبادل.
- تستخدم جداول المؤسسات والفروع والصفوف ومستخدمي الفروع دوال `can_manage_*` ذات `security definer` للتحقق من دور التطبيق وعضوية المؤسسة عبر سلسلة الآباء.
- تمنح `branch_users` أعمدة CRUD اللازمة فقط، ولا تمنح عميل Flutter كتابة `profile_id` أو الحذف الفعلي.
- تسمح RLS بقراءة جلسات الذكر للمستخدم المالك فقط، وتحصر الكتابة في RPC الموثقة.
- تسمح RLS بإدارة `managed_wirds` فقط لمن يستطيع `can_manage_branch(branch_id)`، ولا يمنح العميل حذفًا فعليًا.
- يستطيع الطفل قراءة ورده وتقدمه ومكافآته فقط عبر ربط `branch_users.profile_id = auth.uid()`؛ كل الكتابة عبر RPC.
- لا يستطيع الطفل قراءة نقاط أو أيام التزام أو أوسمة طفل آخر؛ التقرير العابر للأطفال متاح فقط عبر `get_branch_child_rewards` بعد صلاحية مدير الفرع.
- يستطيع الموظف قراءة سجله وورده المكلّف عبر ربط `profile_id`، ويستطيع المعلم قراءة صفه وأطفاله فقط عندما يطابق `class_id` صفه المرتبط.
- تعمم `apply_assigned_wird_increment` احتساب تقدم الورد على الطفل والموظف، ولا تمنح مكافآت إلا عندما يكون المكلّف من نوع `child`.
- تجمع `get_branch_daily_metrics` عمليات `managed_wird_mutations` ضمن حدود اليوم المرسلة، وتتحقق أولًا من `can_manage_branch` قبل إرجاع أي مؤشر.
- تجمع `get_central_daily_metrics` الفروع النشطة للمؤسسة المسموح بإدارتها فقط. وتحسب نسبة المشاركة من المستخدمين النشطين، ونسبة الإكمال من المشاركين، مع إبقاء إجمالي الأذكار مؤشرًا وصفيًا غير مستخدم في التصنيف.
- لا يمنح العميل حذفًا فعليًا أو إدارة مباشرة لعضويات المؤسسات؛ يسمح فقط بالأعمدة اللازمة للإنشاء والتعديل والحذف الناعم.

## 6. مخطط Drift المحلي

مخطط Drift المنفذ في الإصدار `2` يحتوي `dhikr_sessions` و`dhikr_mutations`. أضاف الإصدار الثاني `managed_wird_id` الاختياري مع ترحيل يحفظ العدادات السابقة. ويظل المخطط المستهدف لاحقًا محتويًا على نسخ محلية من `user_preferences`, `wird_plans`, `plan_items`, `daily_days`, `daily_items`, `reminders`، والمحتوى الموثق اللازم. لا تدخل بيانات المؤسسات ومستخدمي الفروع Drift لأن CRUD الإداري يتطلب اتصالًا مباشرًا.

| الحقل | الغرض |
|---|---|
| `sync_state` | `synced`, `pending`, `failed` |
| `remote_updated_at` | آخر نسخة بعيدة مطبقة |
| `deleted_at` | حذف ناعم ينتظر المزامنة |

ويضم جدولًا محليًا فقط باسم `sync_queue`:

| الحقل | الغرض |
|---|---|
| `operation_id` | UUID ثابت عبر كل إعادة محاولة |
| `entity_type` / `entity_id` | الكيان المستهدف |
| `operation_type` | نوع العملية |
| `payload` | JSON للعملية بعد التحقق المحلي |
| `attempt_count` | عدد المحاولات |
| `next_attempt_at` | موعد المحاولة التالية |
| `last_error_code` | رمز منقح للخطأ |
| `created_at` | ترتيب العمليات المتصلة |

لا تتم مزامنة `sync_queue` بوصفه جدول بيانات؛ تستهلكه طبقة المزامنة فقط. كل تغيير في مخطط Drift يرفع `schemaVersion` ويضيف ترحيلًا واختبارًا له.

## 7. قواعد المزامنة والتعارض

- UUID يولد قبل أول كتابة محلية ويظل نفسه في Supabase.
- تقدم الورد يستخدم عملية ذرية ذات `operation_id` بدل استبدال قيمة عمياء.
- تعديلات البيانات الوصفية تستخدم نسخة أو `updated_at` لكشف التعارض؛ سياسة الحسم التفصيلية تحتاج اعتمادًا قبل التنفيذ.
- الحذف الناعم ينتقل عبر `deleted_at` ثم ينظف نهائيًا وفق سياسة الاحتفاظ.
- نتيجة `connectivity_plus` لا تسمح بحذف العملية من الطابور؛ لا تزال العملية معلقة حتى يؤكد Supabase نجاحها.
- يسحب العميل التغييرات باستخدام مؤشر مستقر يجمع `updated_at` و`id` حتى لا يفقد صفوفًا لها التوقيت نفسه.

## 8. قواعد السلامة والحذف

- حذف المستخدم يُنفذ وفق سياسة حذف واضحة، ويفضل عبر مهمة خلفية قابلة للتدقيق.
- لا تستخدم `CASCADE` تلقائيًا قبل مراجعة أثره على سجل الإنجاز والمحتوى الموثق.
- أرشفة الخطة هي العملية المعتادة بدل الحذف الفعلي.
- تحفظ السجلات اليومية لقطات الهدف والعنوان حتى لا تعيد التعديلات كتابة التاريخ.
- جميع التحديثات يجب أن تضبط `updated_at` في طبقة التطبيق أو قاعدة البيانات.
- تُختبر الاستعادة من النسخ الاحتياطية دوريًا، لا مجرد إنشاء النسخ.

## 9. قرارات مخطط مؤجلة

- مزود تسجيل الدخول داخل Supabase Auth والحقول الإضافية المطلوبة.
- السماح بحسابات الضيوف من عدمه.
- سياسة تجاوز `progress_value` للهدف.
- تمثيل أهداف القرآن: صفحة، آية، سورة، أو نطاق مركب.
- سياسة الاحتفاظ بسجلات التذكير والأخطاء.
## فلاتر التقارير الإدارية

لا تضيف المرحلة 21 جدول بيانات جديدًا؛ تعتمد على لقطات `managed_wirds` وعلاقات `branch_users` و`classes` و`wird_programs` و`dhikr_definitions`، وعلى `managed_wird_progress` لتحديد حالة الإنجاز.

### `get_administrative_report_filter_options`

تعيد خيارات الفروع والصفوف والمستخدمين والأوراد والأذكار للمؤسسة بعد التحقق من `can_manage_organization`. تحمل خيارات الصف والمستخدم معرفات الآباء اللازمة لتقييد القوائم التابعة في الواجهة.

### `get_administrative_completion_report`

تقبل فترة شاملة لا تتجاوز 366 يومًا، ومعرفات اختيارية للفرع والصف والمستخدم والورد والذكر، إضافة إلى الدور وحالة الإتمام. تنفذ جميع الشروط داخل الاستعلام بعد التحقق من صلاحية المؤسسة. تُشتق الحالة كالتالي: `completed` من حالة التكليف، و`in_progress` عند وجود عد موجب، وإلا `not_started`.

يدعم الفهرس الجزئي `managed_wirds_administrative_report_idx` الوصول إلى التكليفات النشطة أو المكتملة حسب الفرع وتاريخ التكليف والمستخدم، مع استبعاد المحذوفات والمسودات.
