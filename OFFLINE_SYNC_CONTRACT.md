# Offline Sync Contract — Rawdat Wird

الحالة: **OWNER-APPROVED BEHAVIORAL CONTRACT — GRACE-WINDOW DURATION DEFERRED TO G7**  
هذه وثيقة سلوك، لا تختار SQL أو مكتبة جدولة أو خوارزمية retry نهائية.

## 1. invariant المستخدم

القيمة المعروضة للحساب والورد هي:

`server acknowledged count + pending local delta`

مع سقف `target_count`. يحتفظ العميل بآخر قيمة أقرها الخادم وبالزيادات المحلية غير المعترف بها بصورة منفصلة. لا تستبدل قراءة خادمية أقدم زيادات محلية صحيحة.

## 2. عقد الضغطة

1. كل ضغطة صحيحة تنفذ معاملة محلية ذرية تضيف الزيادة إلى التقدم المحلي/الدفعة المعلقة قبل تحديث الواجهة.
2. تظهر النتيجة فورًا ولا تنتظر شبكة أو connectivity check.
3. لا يقبل العداد زيادة تتجاوز snapshot الهدف.
4. لا ينشأ طلب شبكة متزامن لكل ضغطة.
5. يبقى العدد والـoutbox بعد إغلاق التطبيق أو قتله أو إعادة تشغيل الهاتف.

## 3. عقد الدفعات والـidempotency

- تجمع الضغطات المعلقة في حدث مزامنة أو دفعة delta مناسبة، ولكل حدث `event_uuid` ثابت لا يتغير عبر retry.
- قبول الخادم للحدث وتحديث التقدم المجمع وإنتاج الإكمال/المكافأة يقعان في معاملة واحدة.
- إعادة `event_uuid` المقبول تعيد acknowledgment ولا تزيد العدد ولا تكرر الإكمال أو النقاط أو الوسام.
- لا يحذف العميل الحدث قبل acknowledgment صريح. انقطاع الرد بعد قبول الخادم يؤدي إلى إعادة المعرف نفسه.
- الخادم لا يعتمد `client_timestamp` وحده لقبول المدة أو اليوم أو الصلاحية؛ وقت الخادم وحالة التكليف هما المرجع، مع حفظ وقت العميل كدليل تشخيصي فقط.

## 4. المصالحة وتعدد الأجهزة

1. الخادم canonical بعد المصالحة، لكن لا يجوز له إسقاط delta محلي غير acknowledged بصمت.
2. كل جهاز يحمل `device_id` غير إعلاني وevent UUIDs مستقلة.
3. الأحداث المقبولة من جهازين تجمع idempotently حتى الهدف؛ بلوغ الهدف لا يجعل الأحداث المقبولة سابقًا مفقودة، والجزء فوق الهدف يسجل كنتيجة capped/rejected واضحة بدل الصمت.
4. إذا انتهت مدة الورد أثناء انقطاع الجهاز، يقرر الخادم قبول الزيادة بناءً على سياسة نافذة سماح ثابتة. مدة النافذة مؤجلة صراحة إلى G7؛ يجب إثبات أهلية الحدث دون الثقة بوقت العميل وحده، باستخدام دليل sequence/session أو بديل موثوق معتمد في G7.
5. عند تبديل الحساب، يعزل outbox حسب الهوية؛ لا يزامن حساب حدث حساب آخر.

## 5. الفشل والاستعادة

- network/auth/server failures تبقي الحدث pending مع سبب وآخر محاولة، وتستخدم retry مع backoff.
- اتصال `connectivity_plus` محفز للمحاولة فقط وليس إثبات نجاح.
- فشل دائم قابل للإصلاح يظهر حالة واضحة ومسار retry/reauth، ولا يحذف التقدم.
- المصالحة اليدوية للإدارة، إن لزمت، تنشئ تصحيحًا مدققًا ولا تعدل/تحذف حدثًا مقبولًا خفية.
- logout أو حذف التطبيق لا ينبغي استخدامه كمسار طبيعي للتخلص من pending progress؛ سياسة الجهاز المشترك في `STUDENT_AUTH_POLICY.md` واجبة.

## 6. الإكمال والمكافآت

- الانتقال الأول إلى completion له مفتاح استحقاق فريد.
- دفتر النقاط ووسام أول ورد والاستمرارية تستخدم مفاتيح idempotency مستقلة مشتقة من instance/day/rule.
- retry أو وصول جهاز ثانٍ لا يعيد منح نتيجة سبق منحها.
- لا تمنح المكافآت من العدد المحلي غير المتزامن بوصفها حقيقة نهائية؛ يمكن عرض حالة متوقعة بوضوح حتى إقرار الخادم.

## 7. ما يجب إثباته في G6

- عد فوري في وضع الطيران.
- معاملة محلية لا تفصل العدد عن outbox.
- بقاء `20 + 30 = 50` بعد إغلاق قاعدة البيانات/التطبيق وإعادة فتحه.
- العزل بين حسابين على جهاز مشترك.
- عدم تجاوز الهدف وسلامة الاستعادة بعد crash في نقاط حرجة.

## 8. ما يجب إثباته في G7

- دفع دفعة delta لا طلب لكل tap.
- إعادة الحدث نفسه بعد فقد الرد لا تضاعف العدد.
- تسليم أحداث متعددة وبترتيب مختلف ينتج المجموع الصحيح.
- جهازان دون اتصال لا يفقدان تقدمًا مقبولًا، ولا يتجاوز الناتج الهدف.
- انتهاء الورد، نافذة التسليم، تعارض ساعة العميل، وإعادة المصادقة لها نتائج محددة.
- completion/points/badges لا تتكرر.
- تبقى الأحداث الفاشلة قابلة للاستعادة، وتطابق القيمة النهائية الخادم وتقارير الإدارة.

## 9. قرارات تنفيذية مؤجلة بأمان

حجم الدفعة، debounce، نوع backoff، بنية جداول Drift، اسم RPC، مدة الاحتفاظ بالأحداث acknowledged، وآلية اكتشاف الجهاز. يجب أن تختار في G6/G7 ضمن invariants أعلاه وتوثق باختبارات.
